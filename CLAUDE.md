# UA SIM — Union Arena Simulator

Fan-made web simulator สำหรับเกมการ์ด UNION ARENA — vanilla HTML/CSS/JS ไม่มี build step
เล่นจริง: **https://bright4479.github.io/ua-sim/** (deploy อัตโนมัติเมื่อ push ขึ้น `main`)
คุยกับผู้ใช้เป็น**ภาษาไทย** (UI ของเกมก็เป็นภาษาไทย)

## เป้าหมายปัจจุบัน (งานต่อเนื่องหลาย session)

**ทำให้ effect ของการ์ดใช้งานได้จริงทุกใบ ทุก series รวมถึง Raid boss**
ลำดับที่ตกลงกับผู้ใช้: จับการ์ดที่มี pattern คล้ายกันก่อน (generic) → แล้วค่อยไล่ effect เฉพาะตัว/Raid boss ทีละใบ

สถานะล่าสุด (2026-07-26): การ์ดที่มี effect text 6,258 ใบ มี automatic handling แล้ว **~97.5%** (ตัวเลขนี้ผ่านการแก้บั๊กที่เคยทำให้เฟ้อมาแล้ว 3 รอบ — ดู "audit รอบ 2" ด้านล่าง; เดิม 98.3% *โดยตั้งใจ* — แก้บั๊ก quoted-grant ที่เคยนับ keyword ถาวรให้การ์ด 89 ใบผิดๆ ดูหัวข้อ "รอบ keyword audit" ด้านล่าง; ตัวเลขเดิมคือตัวเลขที่เฟ้อจากบั๊ก) (วัดด้วย `node tools/coverage-total.mjs` — มี ranking series ที่แย่สุดให้เลือกทำต่อ; ใช้ `node tools/coverage-report-full.mjs` เพื่อดูตารางทั้ง 55 series (เพิ่ม `--residual <SERIES>` เพื่อไล่ดูการ์ดที่ยังมี bonus text ไม่ได้ script) + "bonus text ค้าง" ของ Raid boss ด้วย)
- **MCR: 174/178 (98%)** · **EVA: 106/108 (98%)** · **HTR: 177/179 (99%)** · **ARK: 168/174 (97%)** · **CGH: 168/169 (99%)** · **AND: 71/72 (99%)** · **SLG: 74/75 (99%)** · **JJK: 176/179 (98%)** · **BLC: 171/173 (99%)** · **TSK: 163/168 (97%)** · **KMY: 176/184 (96%)** · **KGR: 74/78 (95%)** · **SMD: 71/77 (92%)** · **GMR: 73/75 (97%)** · **BLK: 112/113 (99%)** · **KMR: 197/202 (97%)** · **OPM: 104/108 (96%)** · **KIN: 71/73 (97%)** · **YYH: 107/107 (100%)** · **HIQ: 100/102 (98%)** · **TLR: 76/76 (100%)** · **GIM: 139/141 (99%)** · **SNF: 78/78 (100%)** · **SYN: 99/100 (99%)** · **NGR: 71/74 (96%)** · **TRK: 106/107 (99%)** · **NIK: 154/158 (97%)** · **BCV: 99/100 (99%)** · **RNK: 107/109 (98%)** · **TOA: 106/106 (100%)** · **KJN: 74/75 (99%)** · **GNT: 99/101 (98%)** · **MST: 74/74 (100%)** · **SHY: 76/78 (97%)** · **CSM: 75/75 (100%)** · **AOT: 168/169 (99%)** · **RLY: 74/74 (100%)** · **KGD: 108/108 (100%)** · **REZ: 100/103 (97%)** · **BTR: 107/107 (100%)** · **MHA: 165/171 (96%)** · **WBK: 76/76 (100%)** · **IMS: 212/219 (97%)** · **KJ8: 71/73 (97%)** · **MGS: 103/106 (97%)** · **MSS: 77/78 (99%)** · **TKG: 103/104 (99%)** · **FMA: 101/102 (99%)** · **SAO: 168/169 (99%)** · **TKN: 98/98 (100%)** · **IYS: 75/75 (100%)** · **BAC: 103/104 (99%)** · **CGD: 74/74 (100%)** · **DST: 100/103 (97%)** · **MMM: 103/103 (100%)**
- **งานเรียงตาม series ที่แย่สุดก่อน** (ผู้ใช้สั่ง "/goal" ให้ทำต่อเนื่องจนกว่าจะจบ) — ทำ HTR→ARK→CGH→AND→SLG→JJK→BLC→TSK→KMY→KGR→SMD→GMR→BLK→KMR→OPM→KIN→YYH→HIQ→TLR→GIM→SNF→SYN→NGR→TRK→NIK→BCV→RNK→TOA→KJN→GNT→MST→SHY→CSM→AOT→RLY→KGD→REZ→BTR→MHA→WBK→IMS→KJ8→MGS→MSS→TKG→FMA→SAO→TKN→IYS→BAC→CGD→DST→MMM→MCR→EVA→SMD→KMR→KMY→GNT→HTR→AND แล้ว ถัดไป: **NGR (96%)** — เหลือ KMY/NGR/OPM/MHA เป็น series ที่ยังไม่ถึง 97% (SMD/KGR ยังต่ำกว่าแต่ทั้งคู่เจอ deep-dive แล้วพบว่าที่เหลือทั้งหมดเป็น mechanic ใหม่ที่ไม่คุ้มสร้าง infra สำหรับ 1-2 ใบ — ข้ามไปแล้ว) — ดูจาก `coverage-total.mjs` ranking สดทุกครั้ง (อย่าพึ่งลิสต์เก่าใน CLAUDE.md เพราะเปลี่ยนทุกรอบ)
- **⭐ รอบ keyword audit (2026-07-25) — บั๊กใหญ่ 2 ตัวที่ซ่อนมาตั้งแต่ต้นโปรเจค**: หลังเจอบั๊ก `[Sniper]` ในรอบ AND เลยไล่ audit keyword ทั้งหมดอย่างเป็นระบบ (สคริปต์อยู่ใน scratchpad: เทียบ "จำนวนการ์ดที่ข้อความมีแนวคิดนั้น" vs "จำนวนการ์ดที่ flag ถูกเซ็ตจริง") พบ:
  1. **`[Sniper]` ไม่เคยทำงานเลย** — parser จับ `[Snipe]` (4 ใบ) แต่ข้อมูลสะกด `[Sniper]` (113 ใบ) แก้เป็น `/\[Sniper?\]/i` → keyword ทำงานจริง 107 ใบ
  2. **quoted-grant ถูกนับเป็น keyword ถาวร** — ข้อความในเครื่องหมายคำพูด เช่น `[Main][Discard 1][1 Per Turn] This character gains "This character cannot be blocked by characters with BP 4000 or more" during this turn.` คือความสามารถที่ **มอบให้ชั่วคราวและมีค่าใช้จ่าย** แต่ `parseKeywords` อ่านทั้งก้อนเลยแจก keyword นั้นให้ **ถาวรและฟรี** ตั้งแต่เทิร์นแรก — กระทบ **89 ใบใน main set** (8 flags: `unblockableBPMin` 72, `unblockableBP` 42, `untargetable` 11, `snipe` 10, `impact` 4, `snipeMaxBP` 4, `dmg` 3, `immuneBpReduction` 2) แก้โดยเพิ่ม `fxStatic` (= effect ที่ตัดข้อความในเครื่องหมายคำพูดออก) แล้วให้ flag แบบ static ทุกตัวอ่านจาก `fxStatic` เท่านั้น (แต่ raid/alsoTreatedAs ยังอ่าน `fx` เต็ม — ตรวจแล้วว่าไม่กระทบ) พร้อมกันนั้นเพิ่ม generic pattern ใหม่ `parseUnblockableGrant`/`matchSelfUnblockableGrant`/`matchChosenUnblockableGrant` ใน common.js ให้ grant พวกนี้ทำงาน**อย่างถูกต้อง**ผ่าน `tempUnblockableBP`/`tempUnblockableBPMin` แทน (รองรับทั้ง `[Main]`+cost, `[On Play]`/Event, `[When Attacking]`) — และขยาย regex `unblockableBP`/`BPMin` ให้รองรับสำนวนที่ตกหล่น ("cannot **blocked** by" ไม่มี be, "by **a character** with", "of N BP", "N or more BP", "BP2000 or lower characters", "or greater")
  - รองรับ grant ครบ 4 ทาง: `[Main]`+cost (จ่าย `[Discard N]`/`[Rest this card]`/เช็ค `[1 Per Turn]` จริง), `[On Play]`, Event (`findSeg` + จั่วในประโยคเดียวกันด้วย), `[When Attacking]`, และ **conditional passive self-grant** ("If this character's BP is N or more / required energy is N or more, this character gains "..."") ที่ประเมิน**สดตอนโจมตี** ผ่าน `matchCondSelfUnblockableGrant` (เช็คก่อน `findClause('[When Attacking]')` เพราะการ์ดกลุ่มนี้ไม่มี marker นั้น; รองรับเงื่อนไข BP / required energy / "If you used an Event Card during this turn" ผ่าน `p._eventsUsedThisTurn` — cond รับ `(unit, p)`) — ตรวจแล้วว่าเงื่อนไขถูกเคารพจริง (BAC-087 BP 4000 → ไม่ได้ grant, พอ +6000 → ได้)
  - **ผลรวม**: coverage ตัวเลข **98.3% → 98.0%** เป็นการลดที่ถูกต้อง (ตัวเลขเดิมเฟ้อเพราะการ์ดถูกนับ covered จาก keyword ที่ผิด) ที่เหลือเป็น grant ของความสามารถอื่นที่ไม่ใช่ unblockable (เช่น quoted `[Damage (2)]` แบบมีเงื่อนไข) ซึ่งยังไม่ได้ทำ
  - **ต่อยอด (รอบเดียวกัน)**: ขยาย quoted-grant ให้ครอบคลุม**ความสามารถอื่นนอกจาก unblockable** ด้วย — `GRANTED_ABILITIES` table + `matchAbilityGrant`/`resolveAbilityGrant` map ข้อความในเครื่องหมายคำพูดไปยัง temp field ที่มีอยู่แล้ว (`tempSnipe`/`tempImpact`/`tempDmg`/`tempUntargetable`/`tempCannotAttack`/`tempCannotBlock`/`tempCannotMove`/`_grantedAttackDraw`/`_grantedUnblockedDraw`/`_grantedOnWinDraw`) พร้อม**ตรวจฝั่งเป้าหมายจากประโยค** ("on your opponent's ... give them X" → ศัตรู) wired เข้า onPlay/onEvent/onAttack/onMain เหมือนกัน
    - **⚠️ กับดักที่เจอ**: `tempCannotAttack`/`tempCannotBlock`/`tempCannotMove` **ไม่มี reset ใน endPhase** (ออกแบบให้เคลียร์ด้วย `scheduleDelayedAction` เอง) ถ้า grant แล้วไม่ schedule clear จะกลายเป็นถาวร — เป็นบั๊กประเภทเดียวกับที่กำลังแก้อยู่! แก้โดยให้แต่ละ entry มี `clear` คู่กับ `apply` แล้ว schedule ที่ `G.turn+1` ("during this turn") หรือ `+2` ("until the start of your next turn") — **เวลาเพิ่ม temp field ใหม่ ต้องเช็คก่อนเสมอว่ามันถูก reset ที่ไหน**
    - ทำ `scheduleDelayedAction` ให้ทน `G._delayedActions` ที่ยังไม่ init (`(G._delayedActions ||= [])`) — กันพังเวลาถูกเรียกจาก synthetic board ของเครื่องมือวัด
    - **forced-block ทำแล้ว (ปิด gap เดิมตั้งแต่ KGR/SNF)**: เพิ่ม keyword `kw.mustBeBlocked`/`kw.mustBlock` + temp คู่หู `tempMustBeBlocked`/`tempMustBlock` (reset ใน endPhase) — บังคับใน blocker-selection ของ `attackPhase`: ถ้ามี defender ที่ติด `mustBlock` จะ**จำกัดตัวเลือกเหลือเฉพาะตัวนั้น**, ถ้า attacker ติด `mustBeBlocked` จะ**ปฏิเสธการบล็อกไม่ได้** (ถ้า controller ส่ง null กลับมา engine เลือกตัวที่ BP สูงสุดให้อัตโนมัติ) ครอบคลุม 2 ใบ static + 27 ใบที่เป็น quoted grant
    - **⚠️ กับดักซ้ำรอบที่ 2**: การ์ดบางใบพิมพ์ forced-block ไว้ใน **`[Main]` ability ที่มี cost** (HTR-1-093 `[Main][Pay 1 AP]`, NGR-1-016 `[Main][Discard 1]`) — ข้อความไม่ได้อยู่ในเครื่องหมายคำพูด เลยรอด `fxStatic` มาได้ ถ้าไม่กันจะแจกฟรีถาวรอีก แก้โดยเช็ค static keyword เฉพาะ segment ที่**ไม่ได้เริ่มด้วย `[Main]`** (`passiveSegs`) — บทเรียน: quoted text ไม่ใช่ทางเดียวที่ ability ถูก grant, clause ที่มี cost ก็นับด้วย
    - ยอมรับ approximation 2 จุด: KJN-1-001 ("ต้องบล็อกด้วย character ที่ BP สูงกว่า") บังคับบล็อกแต่ไม่บังคับเงื่อนไข BP, BCV-1-083 (tier bullet "4 or more:") ให้เป็น static ตลอด; และ 2-target forced block ("the chosen opponent's character must block the chosen character's attack" — KGR-1-069/TRK-1-028/OPM-1-098) ยังไม่ทำ เพราะต้องผูกคู่ attacker-blocker เฉพาะคู่
  - **หมายเหตุเรื่องการวัด**: การ์ดที่เป็น conditional grant จะยัง**รายงานว่า uncovered** อยู่ ถ้าเงื่อนไขไม่เป็นจริงบนกระดานสังเคราะห์ของเครื่องมือวัด (เช่น SHY-1-010 ต้องมี Event ถูกใช้ก่อน) — logic ถูกต้องแล้วในเกมจริง อย่าไล่แก้ตัวเลขจนเสียความถูกต้อง
  - **บทเรียน**: เครื่องมือวัด coverage **จับบั๊กประเภทนี้ไม่ได้เลย** (การ์ดยังนับ "covered" อยู่) ควรไล่ audit เป็นระยะว่า keyword regex แต่ละตัวแมตช์กี่ใบ เทียบกับจำนวนที่ข้อความมีแนวคิดนั้นจริง และ **ข้อความในเครื่องหมายคำพูด = ความสามารถที่ถูก grant เสมอ ไม่ใช่ keyword ของการ์ดใบนั้น**
- **⭐ รอบ replacement-effect (2026-07-26) — ปิด gap "กันการ์ดถูกนำออกจากสนาม" ที่ยอมรับ skip มาตั้งแต่ ARK/RNK/MHA/REZ/GNT**: เพิ่ม hook ใหม่ **`Effects.onBeforeLeaveField(G,p,leavingUnit,ctx,selfUnit) -> truthy = ยกเลิกการนำออก`** ยิงจาก `sidelineUnit` **ก่อน** state เปลี่ยน ไปยังทุกยูนิตที่ผู้เล่นคุม (ตัวที่จะออกจากสนามเป็นตัวสุดท้าย) — hook จ่าย cost เองแล้ว return true เพื่อบอกว่า "ไม่ต้องเอาออก" (convention เดียวกับ `onWinBattle` ที่ return true เพื่อ override)
  - **`ctx = {reason, byOpponent, fromTrigger}`** — `byOpponent` **อนุมานจากว่าเป็นเทิร์นใคร** ไม่ได้ thread ผ่าน call site (มี ~50 ไฟล์ที่เรียก `sidelineUnit(p,u,'effect')` retrofit ไม่ไหว): ยูนิตถูกเอาออกด้วย effect ในเทิร์นของเจ้าของเอง = effect ตัวเอง, นอกนั้น = ของฝ่ายตรงข้าม
  - **จุดที่การอนุมานพลาดและวิธีแก้**: การ์ดอย่าง Kirishima-074 เขียน "[Your Turn] ... by your opponent's effect" — ในเทิร์นเราเอง effect ฝ่ายตรงข้ามที่เอายูนิตเราออกได้คือ **Trigger** (เช่น [Special] ที่ retire ศัตรู) แต่การอนุมานจะได้ `byOpponent=false` ทำให้การ์ดตายสนิท → เพิ่ม `G._resolvingTrigger` counter รอบ `resolveTrigger` (แยก `resolveTriggerInner` ออกมาเพื่อใช้ try/finally) แล้ว `byOpponent = ownerไม่ใช่active || fromTrigger`
  - **re-entrancy guard `G._resolvingReplacement`**: จำเป็นเพราะ hook ส่วนใหญ่ "retire ตัวเองแทน" ซึ่งเรียก `sidelineUnit` ซ้อน — ถ้าไม่กันจะวนไม่รู้จบเมื่อมีการ์ดกันให้กัน 2 ใบ (ทดสอบแล้ว: Ace 2 ใบบนสนาม retire ใบแรก → จบใน 1ms, ใบแรกรอด ใบสองแทนที่)
  - ทำได้ 6 ใบ: Kirishima-074 (กันตัวเอง), Midoriya-093 (จ่าย Life 1 แทน), Ace-1-106 (retire ตัวเองแทน, ยกเว้น BP-0 — ทดสอบแล้วว่า `reason==='bp0'` ไม่ถูกแทน), Shikijo-013, Yachiho-034 (วางนอน+ทิ้ง 2), White Goreinu-2-026 (retire ตัวเอง + ย้าย Goreinu ไป Energy Line) — ยัง skip: REZ Beatrice-074 (เป็น **grant** ให้การ์ดอื่น ต้องแปะ watcher ชั่วคราว), MHA Shinomori-2-030 (เงื่อนไขซ้อนหลายชั้น)
  - **ข้อจำกัดที่ยอมรับ**: แยก "character effect / Event effect / Trigger effect" จากกันไม่ได้ (นอกจาก trigger) การ์ดที่ระบุชนิดเจาะจงจะถือว่า "effect ฝ่ายตรงข้ามอะไรก็ได้"; และ hook ยิงเฉพาะ `sidelineUnit` (retire) ยังไม่รวม `returnUnitToHand`
- **⭐ รอบ Life→Outside reactive (2026-07-26)**: เพิ่ม hook **`Effects.onLifeToOutside(G,p,card,selfUnit)`** ยิงจาก engine ตรงจุดที่การ์ด Life ถูกเปิดจากความเสียหายแล้วลง Outside Area (หลัง Trigger resolve แล้ว) broadcast ไปทุกยูนิตของเจ้าของ Life — ปิด gap "Life-card-trigger-resolution reactive" ที่ยอมรับ skip มาตั้งแต่ NIK/SHY/EVA ทำได้ 7 ใบ: NIK Dorothy-042/Noah-046, SHY Keheheh-009/012/Tzveta-018, EVA Asuka-087/Mari-092 (เงื่อนไขแยก 2 แบบ: `card.trigger === 'Draw'` กับ `!card.trigger`; ทุกใบเป็น [Opponent's Turn] จึง gate ด้วย active player) — ทดสอบแล้วว่ายิงจริงในเกมบอท (SHY 8 ครั้ง/10 เกม) และไม่ยิงในเทิร์นเจ้าของเอง
  - **⚠️ cluster นี้ทำได้แค่ส่วนเดียว**: กลุ่ม "reactive ต่อการถูก mill/discard จาก deck หรือมือ **โดยเอฟเฟกต์ของการ์ดใบใด**" (IMS/KMR/KGR/DST ~19 ใบ) **ยังทำไม่ได้** เพราะไม่มี choke point — มี **214 จุด** ในไฟล์ effect ที่เขียน `p.sideline.push(...)`/`p.removal.push(...)` ตรงๆ (เทียบกับ 8 จุดใน common.js) การจะรู้ "ใครเป็นคนทำ" ต้อง refactor ให้ผ่าน helper กลางทั้งหมด — ประเมินไว้ตอนแรกว่าเป็น cluster เดียวกันแต่จริงๆ แพงกว่ามาก ต้องตัดสินใจก่อนว่าจะ refactor ไหม
- **⭐ audit รอบ 2 (2026-07-26)** — ไล่ 4 มุม พบบั๊กใหญ่อีก 1 + edge case 1 + บั๊กรายใบ 1:
  1. **registry key ที่ไม่มีการ์ดจริง / ประกาศซ้ำในไฟล์เดียวกัน** → สะอาดทั้งคู่ (2,108 entries)
  2. **⚠️ keyword ที่มาจาก `[Main]` clause ล้วน = 123 instance** (impact 75, dmg 25, doubleAttack 14, snipe 8, doubleBlock 1) — บั๊กประเภทเดียวกับ quoted-grant แต่**ไม่ได้อยู่ในเครื่องหมายคำพูด** เช่น `[Main][Discard 2][1 Per Turn] this character gains [Impact (1)] during this turn` → engine แจก [Impact] ถาวรฟรีตั้งแต่เทิร์นแรก แก้โดยให้ static keyword ทุกตัวอ่านจาก `fxPassive` (= ตัด segment ที่ขึ้นต้นด้วย `[Main]` ออก) + เพิ่ม generic `matchBracketGrant` ใน `matchOnMain` ให้ grant ทำงานถูกผ่าน temp field (เพิ่ม `unit.tempDoubleAttack` ใหม่คู่กับ `kw.doubleAttack`) — ตัวเลขจึงลง 98.2%→98.0% แบบถูกต้อง
  3. **edge case: เครื่องหมายคำพูดไม่ balance** (NGR-1-072 มี `"` เกินมาตัวเดียว) ทำให้ regex `/"[^"]*"/g` จับข้ามไปหา `"` ของ clause ถัดไป **แล้วกลืน `@[Main]` ที่คั่นอยู่** → clause นั้นเลยไม่ถูกกรอง แก้โดย **split '@' ก่อน แล้วค่อยตัดเครื่องหมายคำพูดทีละ clause** (ปลอดภัยกว่าและกันปัญหานี้ทั้งคลาส)
  4. **temp field ที่ไม่มี reset**: มีแค่ `tempCannotMove/Attack/Block` (ตั้งใจ) แต่ไล่เช็คทุกจุดที่เซ็ตแล้วพบ **IMS-2-070 ลืม schedule clear** → การ์ดเป้าหมาย "ห้ามโจมตีเทิร์นนี้" กลายเป็น**ห้ามโจมตีตลอดเกม** แก้แล้ว
- **⭐ เริ่มงาน residual (2026-07-26)** — ผู้ใช้ตัดสินใจ: ทำ residual ให้ครบทั้ง 1,391 ใบ เรียงจาก series ที่คนเล่นเยอะสุดลงมา · เพิ่ม `node tools/coverage-report-full.mjs --residual <SERIES>` (และ `--residual ALL`) สำหรับไล่ดูรายการ — **ใช้ classifier ตัวเดียวกับตารางสรุป** จึงไม่มีทางรายงานไม่ตรงกัน (เคยเขียนเครื่องมือแยกแล้วได้ 92 ใบ vs จริง 43 เพราะลืมเช็คว่า generic pattern ยิงหรือยัง)
  - **บั๊กเดิมคลาสที่ 4 — conditional grant ที่ไม่ได้อยู่ในเครื่องหมายคำพูดและไม่ได้อยู่ใน `[Main]`**: `If there are 4 or more <Trait:X> cards on your area, this character gains [Impact 1].` → `parseKeywords` แจก [Impact] **ถาวรโดยไม่สนเงื่อนไข** กระทบ **142 ใบใน main set** แก้โดยเพิ่ม `isConditionalGrant` เข้าไปในตัวกรอง `fxPassive`
  - แทนที่ด้วย **generic evaluator ที่ประเมินสด**: `Effects.genericImpactBonus`/`genericDmgBonus` (ต่อเข้ากับ hook `impactBonus`/`dmgBonus` ที่ engine มีอยู่แล้ว) + `Effects.genericKeywordActive(owner,unit,kind)` สำหรับ keyword ที่เป็น flag (`snipe`/`doubleAttack`/`doubleBlock`/`nullifyImpact` — ต่อที่ 4 จุดใน battle resolution + bot.js's snipe filter) ใช้ `countMatching` ตัวเดิมของ bpEvaluator ซ้ำ รองรับเงื่อนไข: ชื่อ/trait บนสนาม, BP threshold (4 สำนวน), required energy, จำนวนการ์ดในมือ/เด็ค/Outside/Remove Area, Life ทั้งสองฝั่ง, การ์ดคว่ำใต้ตัว, same-lane — **ทำได้ 62 ใบ**
  - **ยอมรับการถอยชั่วคราว**: อีก ~80 ใบที่เงื่อนไขเป็นสำนวนเฉพาะตัว (91 clause, เกือบทั้งหมดโผล่ครั้งเดียว) จะ**เสีย keyword ไปก่อน**จนกว่าจะ script รายใบ — เลือกทางนี้เพราะ "เงื่อนไขไม่ผ่าน" ใกล้เคียงความถูกต้องมากกว่า "ติดถาวรฟรี" (ซึ่งเป็นการเพิ่มพลังให้การ์ดเกินจริง) และการ์ดกลุ่มนี้อยู่ในคิว residual ที่กำลังจะไล่ทำอยู่แล้ว — ตัวเลขจึงลง 98.0%→97.5%
  - **residual จริงคือ 1,124 ใบ ไม่ใช่ 1,391** (แก้ false positive 2 รอบ): `residualText` ตัดสำนวน self-gen ("If this character is active, increase the energy it generates by N") ไม่หมด เหลือหางไว้ → รายงาน false residual **182 ใบ** ทั้งที่ engine ทำงานถูกอยู่แล้ว (`selfGenBonus`) แก้ pattern ให้ตัดทั้งประโยค · รอบสองพบอีก 75 ใบ: `residualText` ตัด bracket tag **ก่อน** รัน `COST_DISCOUNT_RES` ทำให้ regex ที่อ้าง `[yellow] or [blue] Card` แมตช์ไม่ได้ — สลับลำดับให้ตัด cost-discount ก่อน
  - **ลำดับ series (ตามความนิยมแฟรนไชส์ — ไม่มีข้อมูล play-rate จริง)**: JJK → KMY → MHA → BLC → HTR → CSM → AOT → SAO → OPM → CGH → ที่เหลือ
  - **⭐ SAO round — generic 3 ตัวที่ปลดล็อคทั้งฐานข้อมูล (residual 976 → 935)**: ตรวจก่อน script ตามบทเรียนเดิม แล้วพบว่าเกือบครึ่งของ SAO residual เป็น measurement artifact หรือ near-miss ของ generic:
    1. **เงื่อนไข "`<A> and <B>` พร้อมกัน"** (18 ใบ ใน 8 series) — `parseKeywordCondition`/`buildBpEvaluator` รองรับแค่ชื่อเดียว → เพิ่ม `RX_ALL_NAMES`/`allNamesPresent` (รองรับสูงสุด 3 ชื่อ) ใช้ร่วมทั้ง BP และ keyword evaluator (ทดสอบ: Alice-012 ได้ +1000 BP และ Kirito-002 ได้ [Double Attack] เฉพาะเมื่อมีครบทั้งคู่)
    2. **tier bullets** ("This character gains all the following effects based on the number of X" + bullet `• N or more:`) — **45 ใบทั้งฐานข้อมูล** เพิ่ม `parseTierBase`/`parseTierBody`/`parseTierRules` ที่ **desugar bullet ให้เป็น rule shape เดิมของ bpEvaluator/keywordEvaluator** แทนการเขียน evaluator ตัวที่สาม (base ที่รองรับ: นับการ์ด/Event ใน Outside/Remove/Life Area, ชื่อ/trait บนสนามหรือในโซนซ่อน (มี distinct), การ์ดคว่ำใต้ตัว, BP ตัวเอง, generated energy) — รับได้ 24/45 ที่เหลือเป็น body แบบ `[On Play]`/`[When Attacking]`/unblockable ที่ต้อง script รายใบ
       - **⚠️ ข้อมูลพิมพ์ bullet โดยไม่มี `•` บ่อยมาก** (NGR/MCR/EVA) และเขียน threshold หลายแบบ ("BP 6000 or more:", "0 card :", "4 or more cards:") → ทำ marker เป็น optional ได้ปลอดภัยเพราะสแกนเฉพาะ segment **หลัง** head clause เท่านั้น
    3. **บั๊กแฝงใน keyword regex**: `\[(Impact|Damage|...)[^\]]*?(\d*)\s*\]` จับเลขไม่ได้เมื่อมีวงเล็บ (`[Damage (2) ]` → lazy quantifier กลืนเลขไปหมด ได้ n=1 เสมอ) แปลว่า **[Damage (2)] แบบมีเงื่อนไขทำงานเป็น Damage 1 มาตลอด** แก้เป็น `[^\]\d]*(\d*)[^\]]*\]`
    - **แก้ classifier ให้ถามตัว evaluator จริงแทนการ copy regex**: `residualText` split clause แล้วเรียก `Effects.hasGenericBp`/`hasGenericKeyword`/`hasGenericGen` ทีละ clause — เป็นต้นเหตุของ false residual หลายรอบที่ผ่านมา (การ copy regex ไปไว้ในเครื่องมือวัดแล้วลืม sync) · เพิ่ม strip "treated as both A and B" (alsoTreatedAs ทำงานอยู่แล้ว) และ "Play this character **and** set it to active" (25/27 ใบ parse ถูกอยู่แล้ว)
  - **AOT**: 27 → **21 ใบ** ทำได้ 5 ใบ — **⚠️ กับดักเดียวกับ CSM แต่คนละ field**: AOT-1-053 [On Retire] ต้องอ่าน `unit.under` (raid source) แต่ `sidelineUnit` เท `under` ลง Outside Area **ก่อน** เรียก `onSideline` → ใช้ **`onBeforeLeaveField` (return false)** จับค่าไว้ก่อน แล้วดึงการ์ดกลับจาก sideline ตอน `onSideline` (ทดสอบแล้ว: กลับเข้ามือจริง ไม่ค้างใน Outside Area) · ใช้ `impactBonus` hook คู่กับ `bpBonus` สำหรับ passive ที่ให้ทั้ง BP และ [Impact] (Sasha-081 gate ด้วย `p._playedTraitsThisTurn`)
  - **CSM**: 14 → **8 ใบ** ทำได้ 6 ใบ — **⚠️ กับดักที่เจอซ้ำ**: CSM-1-047 เช็ค "มีการ์ดคว่ำใต้ตัวไหม" ใน `onSideline` แล้วได้ 0 เสมอ เพราะ `sidelineUnit` เท `counters` ทิ้งไป Outside Area **ก่อน** เรียก `onSideline` → ต้องจับค่าใน `onBeforeLeaveCounters` (return false เพื่อให้ engine เททิ้งตามปกติ) แล้วเก็บใส่ `unit._hadFacedown` — ทดสอบแล้วว่าการ์ดกลับลง Front Line จริงและไม่มีตกค้างในเด็ค/Outside
  - **HTR**: 26 → **19 ใบ** ทำได้ 5 ใบ — ตัวอย่าง `unit._watchers[]` แบบ scope รายเทิร์น (Illumi-050 ทำเครื่องหมายศัตรู: ถ้าถูก retire **เทิร์นนี้** จั่ว 2 — เช็ค `Engine.G.turn !== markedTurn` ใน watcher เพราะ `_watchers` ไม่มี auto-expire), ดึงศัตรู Energy→Front หรือสลับถ้า Front เต็ม — ยัง skip: reveal-any-number tier (007/019), ability-borrowing (037)
  - **BLC**: 22 → **13 ใบ** ทำได้ 9 ใบ — ใช้ `H.battleLoserGoesTo`/`H.unraidTopLayer` ซ้ำ, scaling debuff ตามจำนวนการ์ดที่ส่ง Outside→Remove, "อยู่ตัวเดียวบน Front Line → +1000 BP"
  - **MHA**: 40 → **32 ใบ** ทำได้ 8 ใบ — helper `battleLoserGoesTo(dest)` (ผู้แพ้ battle ไป hand/removal แทน sideline ผ่าน `onWinBattle` return true — **ยกขึ้น `UAEffectHelpers` แล้ว** เพราะ BLC ใช้ซ้ำ) และ `playRaidSourceCard()` (ลงการ์ดที่อยู่ใต้ Raid State โดยราider ยังอยู่บนสนาม — ใช้ `deck.unshift` ก่อนเรียก `playCardFromZone` ตาม pattern เดิม ทดสอบแล้วว่าไม่มีการ์ดตกค้างในเด็ค) — ยัง skip: aura ที่ลบ keyword ของศัตรูทั้งสนาม (Aizawa-035) เพราะ keyword อ่านจาก unit ตรงๆ ไม่มี hook ระดับ defender
  - **KMY**: 33 → **10 ใบ** ทำได้ 23 ใบ — `canPlayFromHand`/`canMoveToFront` (Muzan ต้องมี 20 ใบใน Outside Area), `onAttackPhaseEnd` retire ตัวเอง 2 ใบ, swap Front↔Energy (helper `swapFrontEnergy` ใช้ซ้ำ 2 ใบ), `onWinBattle` return false เพื่อตั้งตัวเองแต่ยังให้ผู้แพ้ถูก sideline ตามปกติ, `onAnyLoseBattle` ที่ต้อง gate `atk === self` (hook ยิงทุกการโจมตีที่แพ้ ไม่ใช่เฉพาะตัวเอง)
  - **JJK (series แรกตามลำดับความนิยม)**: 38 → **13 ใบ** ทำได้ 23 ใบ — [Main] buff ทั้ง Front Line, `pendingDiscount` แบบ except-ชื่อตัวเอง, flat passive BP, bounce, rest+`skipNextStand`+fetch, tier-scaling ตามจำนวน `Sukuna's Finger` ใน Outside Area (Malevolent Shrine + Sukuna-048), mill-then-rest ตอน [On Retire], "ฝ่ายตรงข้ามเลือกการ์ดตัวเองคืนมือ/retire" ผ่าน `enemy.controller.chooseOwnCharacter`
  - เก็บกวาด: ลบ `atk.kw.nullifiedImpact` ที่ engine อ้างถึงแต่ไม่มีใครเซ็ต (dead code — การ negate จริงอยู่บรรทัดถัดไปที่ `defender.kw.nullifyImpact`) กันคนมา "แก้" ผิดทางภายหลัง
- **บทเรียนสำคัญที่ยืนยันซ้ำแล้วซ้ำอีกทุก series**: ก่อน script รายใบ ให้ตรวจสอบก่อนเสมอว่าการ์ดที่ "uncovered" อยู่นั้น จริงๆ แล้วแค่ regex ใน common.js/engine.js ไม่ทนต่อ wording variant เล็กๆ (เว้นวรรคเกิน, "a/an" หาย, ลำดับคำสลับ, ตัวสะกดผิด) หรือเปล่า — ทุก series ที่ทำมาเจอแบบนี้ 30-50% ของรายการเสมอ แก้ regex ทีเดียวได้ coverage เพิ่มโดยไม่ต้อง script อะไรเลย เร็วกว่า script รายใบมาก
- มีชั้น normalizeFx ใน common.js แปลงสำนวนแปลทุกแบบ (series ใหม่สะกดเลขเป็นคำ/"until the end of the turn"/"for the turn"/เลข gen หลุดติดหน้าข้อความ, series เก่าอย่าง HTR แปลหยาบ) ให้ matcher ชุดเดียวใช้ได้หมด
- generic ครอบคลุมแล้ว: on-play **และ on-event ใช้ matcher ชุดเดียวกันแล้ว** (draw/draw+discard/scry/look-at-top-N-fetch/scry-discard-to-top/buff/debuff/rest/retire-conditional/bounce/cond-draw/free-play-from-hand/discard-fetch/fetch-outside — ก่อนหน้านี้ event การ์ดใช้ matcher แยกที่แคบกว่ามาก แก้แล้วรอบ CGH ได้ coverage เพิ่มทันที ~120 ใบทั้งฐานข้อมูลโดยไม่ต้อง script อะไรเลย), passive BP (Your Turn/Opponent's Turn/name/trait/hand-count/"if you have" แบบ singular — `Effects.genericBpBonus` + cache), aura BP hook (`auraBp`, การ์ดหนึ่งให้บวก BP การ์ดอื่นบนสนามเดียวกัน), passive front-line energy generation (`Effects.genericFrontGen` + `kw.frontGen` + per-card `frontGenBonus` hook), untargetable keyword, skip-next-stand mechanic (`unit.skipNextStand`, พบ 63 ใบทั้งฐานข้อมูล), temp unblockable-BP grants (`tempUnblockableBP`/`tempUnblockableBPMin`), hand-based cost-discount coverage-measurement fix (`Engine.hasTextCostDiscount`, รวม "energy consumption -N" แบบไม่มีคำว่า reduce...by ด้วยแล้ว), เมื่อโจมตี (draw+discard/buff), [Main] (selfGenRetire/restBuffOther/discardImpact/restScryTopBottom/payApDraw/restDebuffEnemy), [On Retire] draw(+discard)/rest-enemy/buff-other, keyword ครบ
- Raid boss ตัวใหญ่ (effect หลายชั้น/มี tier) ส่วนใหญ่ยังไม่ได้ script — keyword พื้นฐานทำงาน แต่ bonus text ยังไม่ (ดู `coverage-report-full.mjs` คอลัมน์ "เหลือ script")
- **engine hook รอบ SLG/AND/CGH (เก่า, สรุปย่อ)**: `onAnyAttack`, `unit.tempUntargetable`/`_grantedAttackDraw`, `unit.effectsNullified`, `kw.unblockableBPMin`, `onDefenderWinBattle`, `H.unraidTopLayer`, `p._playedApCostsThisTurn`/`_eventsUsedThisTurn`, `unit.enteredTurn`, `findSeg()` (เชื่อม on-event เข้ากับ on-play matcher — ผลกระทบสูงสุดตอนนั้น)
- **engine hook รอบ ARK/HTR**: `unit.skipNextStand`/`noRetire`/`tempSnipe`/`_playedByEffect`, `auraBp`, `H.lookTopAndDiscard`, `onBeforeLeaveCounters`, `unit._watchers[]`, `onAnyWinBattle`/`onAnyLoseBattle`, `retireAtEndOfTurn`/`noBlock`/`_grantedOnWinDraw`, `_playedTraitsThisTurn`, `onBlock(atkUnit)`
- **engine hook รอบ BLC**: `Engine.scheduleDelayedAction(turn, fn)` (คิวเอฟเฟกต์แบบ "at the start of your opponent's next turn, ..." เช็คใน `startPhase` ทุกเทิร์น), `p._placedToOutsideThisTurn` (นับจำนวนครั้งที่ส่งการ์ดไป Outside Area เทิร์นนี้ — เพิ่ม generic bpEvaluator clause "if you placed a card...to the Outside Area during this turn, +N BP" ด้วย), generic pattern ใหม่: `matchAttackSelfBuff` ("[When Attacking] This character gets +N BP" แบบไม่มีเลือกเป้าหมาย), `matchPlainMillOutside` ("Place N cards from top of deck to Outside Area" แบบไม่มีเงื่อนไข ใช้ได้ทั้ง onPlay/onSideline)
- **engine hook รอบ TSK**: `kw.cannotBlock`/`kw.cannotAttack` (permanent keyword — `cannotAttack` ต้อง filter ออกจาก bot.js's `chooseAttacker` candidate list ด้วย ไม่งั้น bot จะเลือกซ้ำจนวนลูปไม่รู้จบ เพราะ engine แค่ `continue` ข้ามโดยไม่ rest ยูนิต), `Engine.textApDelta` รองรับเงื่อนไขแบบ trait-count แล้ว (ไม่ใช่แค่ named-condition), coverage tool ทั้ง 3 ตัว + scratchpad dump script เพิ่ม `addNamedAlly()` (สร้างพันธมิตรชื่อเดียวกับ `<NAME>` ที่การ์ดอ้างถึงบนกระดานสังเคราะห์ ลดการรายงาน "uncovered" ผิดพลาดจากเงื่อนไข "if there is a &lt;NAME&gt; on your area")
- **engine hook รอบ KMY**: `unit._movedThisTurn` (ติดตามว่า unit นี้ย้าย line เทิร์นนี้หรือไม่ — เซ็ตทั้งใน `moveUnitFree` และ Movement Phase ของ turn loop เอง, reset ทุก `startPhase`), generic bpEvaluator/genEvaluator/AP-discount รองรับเงื่อนไขผสม "`<NAME>` and/or other `<Trait:X>`" แล้ว (ธีม Hashira-synergy ของ series นี้ — `countNameOrTrait()` helper ใหม่ใน common.js), แก้ normalizeFx ให้สลับลำดับคำ "BP+N" เป็น "+N BP" (series นี้สะกดสลับที่บ่อยมาก), ขยาย `mainRestBuffOther`/`onplayBuffOther` ให้รองรับ "another character"/"give it"/"on your field" และ onAttack draw+discard ให้รองรับ "and" เป็นตัวเชื่อมประโยค
- **engine hook รอบ KGR**: `Engine.payApForEffect(p, n)` (จ่าย AP ผ่าน "ability cost" โดยเฉพาะ — ต่างจากจ่าย AP ตอนเล่นการ์ดปกติ — เพิ่ม `p._paidApByEffectThisTurn` counter คู่กัน สำหรับการ์ดที่เช็ค "if you have paid an AP by your character's effect during this turn" ซึ่งพบ 5 ใบใน series นี้), bpEvaluator รองรับเงื่อนไข "if your opponent's Life is N or less, +M BP" แล้ว, แก้ `matchScryDiscardTop` ให้รองรับจุด/comma ทั้งสองตำแหน่ง (ก่อน "Place up to" และหลัง "Outside Area") — เจอบั๊กจากการเดา separator ผิดตำแหน่งเดียวทำให้ 2 ใบไม่ match ทั้งที่ widen มาแล้วรอบก่อน (บทเรียน: debug ด้วย node -e ทดสอบ regex ทีละส่วนเร็วกว่าเดา)
- **engine hook รอบ SMD**: `p.extraDrawUsed` (มีอยู่แล้วจากกติกา extra-draw ปกติ — เพิ่ม `Effects.hasFreeExtraDraw(p)` ให้การ์ด registry ประกาศ `freeExtraDraw: true` แล้วทำให้ Start Phase ข้ามการจ่าย AP ให้), `G._triggerActivatedThisTurn` (มีการ์ดเช็คว่าฝ่ายใดฝ่ายหนึ่งเปิด Trigger ในเทิร์นนี้หรือไม่), `unit.tempRaidable` (การ์ดที่ปกติไม่มี [Raid] ได้รับอนุญาตให้ถูก raid ทับได้ชั่วคราว — เช็คใน `raidTargetsFor`), `kw.unblockableByRaided` (static "cannot be blocked by characters in raided state"), `p._blockEnergyToFrontNextMove` (บล็อกฝ่ายตรงข้ามย้าย Energy→Front ใน Move Phase ถัดไปของเขา) — **พบ mechanic ใหม่ที่ตัดสินใจ skip ทั้ง series**: "face-up card ใน deck/Life" (Sakamoto Days เฉพาะตัว ต้องมี persistent overlay state ใหม่ที่กระทบ UI ด้วย ไม่คุ้มสำหรับ 3-4 ใบ), "activate การ์ดอื่นเปิด Trigger ของตัวเอง" (ต้อง export `resolveTrigger` ออกจาก engine)
- **engine hook รอบ GMR**: `p._dealtDamageThisTurn` (เช็คว่า player นี้สร้างความเสียหายให้ฝ่ายตรงข้ามในเทิร์นนี้หรือไม่ — เซ็ตใน `dealDamage()`) ใช้กับ "if a character on your area dealt damage to your opponent this turn" — series นี้ยังพบ gap เดิมซ้ำ 2 แบบที่เจอมาตลอด: "unblocked attack" detection (onAttack ยิงก่อน block resolve เสมอ) และ "post-attack-resolution" hook ที่ยังไม่มี
- **BLK (Blue Lock) — ไม่มี engine hook ใหม่**: ธีมหลักคือ "face-down card ใต้ character" ซึ่งใช้ `unit.counters` convention เดิมที่มีอยู่แล้ว (เหมือน BLC/TSK/KGR) — เขียน local helper 3 ตัวใน `blk.js` เอง (ไม่ยกเป็น common.js เพราะ wording เฉพาะ series นี้): `hasAnyFaceDown/faceDownHolders/noFaceDownTargets` + `plantUnderSelf`/`moveFaceDown` — ทำได้ 112/113 (99%) เหลือ skip แค่ 1 ใบ (live conditional untargetable เฉพาะ Event-targeting เท่านั้น ไม่คุ้มจะทำเพราะ helper เดิมเป็น all-or-nothing)
- **KMR (Kamen Rider) — series ใหญ่ที่สุดและ effect ซับซ้อนที่สุดที่เจอมา** (202 ใบ, ยอมรับ skip มากกว่าปกติสำหรับการ์ด multi-clause แบบ raid-chain/tier-based-grant ที่ต้องสร้าง infra ใหม่เพื่อการ์ดเดียว) ได้ 187/202 (93%) เพิ่ม `p._revealedNonYellowRaidThisTurn` (เซ็ตใน `H.lookTopAndTake` — ใช้กับการ์ดที่เช็คว่า reveal-แล้วเพิ่มการ์ด non-yellow [Raid] เข้ามือเทิร์นนี้หรือไม่), ขยาย unconditional-BP-modifier regex ให้รองรับเครื่องหมายลบ (`-1500 BP`), ขยาย `kw.entersActive` ให้รองรับ "This X is played as active" (อีกสำนวนหนึ่ง), เพิ่ม local helper `placeSameNeedCost()` ใน `kmr.js` (cost แบบ "ส่งการ์ด Energy เท่ากัน 3 ใบจากมือไป Outside Area" เฉพาะ OOO Combo cards), นำ `unit.counters` มาใช้แบบใหม่สำหรับ Ex-Aid line: "ฝัง" character ทั้งใบ (ไม่ใช่แค่การ์ดคว่ำ) ไว้ใต้ character อื่น
- **engine hook รอบ OPM (One Punch Man)**: `kw.cannotMove` (static "This character cannot move." — filter ออกจาก Movement Phase's move loop ทั้งสองทาง และจาก `moveUnitFree` ด้วย), รวม regex "cannot attack"/"cannot block"/"cannot attack or block" เป็นตัวเดียว (ตั้ง `kw.cannotAttack`/`kw.cannotBlock` จาก match เดียวกัน), ตัวอย่าง live-conditional `tempUnblockableBPMin` grant ที่คำนวณสดทุกครั้งที่โจมตี (Flashy Flash: "6+ Trait:Hero อื่นบนสนาม → ไม่ถูก block โดย BP≥3500" เซ็ตใน `onAttack` ไม่ใช่ static `kw`), แก้ 3 generic regex (`onplayBuffOther`/`onplayDebuffEnemy`/`mainRestBuffOther`) ให้ยอมรับจุด+ตัวพิมพ์ใหญ่ต่อ ("field. It gains") ไม่ใช่แค่คอมม่า+ตัวพิมพ์เล็ก — สไตล์แปลของ series นี้ต่างจาก series อื่น ได้ 104/108 (96%) เหลือ skip 4 ใบ: aura-untargetable grant ("Hellish Blizzard cards immune from targeting"), targeting-tax mechanic (จ่ายการ์ดเพิ่มเพื่อเลือกเป็นเป้าหมาย — พบซ้ำจาก TSK), meta text-replacement effect (Tatsumaki), manual-Raid-trigger mechanic (Metal Bat)
- **engine hook รอบ KIN (Kinnikuman)**: `kw.cannotEnterFront` (static "This card cannot be played to the Front Line." — enforce ใน `playCard` + filter ออกจาก bot.js's front-line play candidates เหมือนแพทเทิร์นเดิมของ `cannotAttack`), `unit.tempCannotMove` (temp คู่หูของ `kw.cannotMove` แบบ "จนถึงต้นเทิร์นหน้าของคุณ" ที่แปะบนยูนิต**ศัตรู** — เคลียร์ด้วย `Engine.scheduleDelayedAction(G.turn+2, ...)` แทนการรอ owner's ของยูนิตเองสตาร์ทเฟส เพราะ "your next turn" หมายถึงเทิร์นของผู้ใช้เอฟเฟกต์ ไม่ใช่เจ้าของยูนิตเป้าหมาย), ตัวอย่าง "BP กลายเป็น X" (ไม่ใช่ +X) ผ่าน `bpBonus` หัก offset กับ printed BP ให้ได้ค่าเป้าหมายพอดี (Stereo-Cassette King) ได้ 71/73 (97%) เหลือ skip 2 ใบ: hand-level reactive ("เมื่อการ์ดใบนี้ถูกทิ้งจากมือ" — ไม่มี hook ระดับมือ), end-of-Attack-Phase hook (gap เดิมซ้ำจาก GMR/KMY/KMR/OPM)
- **engine hook รอบ YYH (Yu Yu Hakusho)**: `unit.genPersist` (ตัวเลขคู่หูของ `frontGenPersist` — extra energy generation "จนถึงต้นเทิร์นหน้า" ไม่ใช่แค่ "เทิร์นนี้" เหมือน `tempGen` — reset ทุก `startPhase` เหมือน `bpPersist`/`frontGenPersist`), ขยาย `kw.untargetable` ให้ครอบคลุมสำนวน "cannot be rested, moved or returned to hand by opponent's effects" (bodyguard-style protection แคบกว่าที่ approximate เป็น full immunity เหมือนที่ทำกับสำนวนเดิมอยู่แล้ว) ได้ **107/107 (100%) — series แรกที่ทำได้ครบ 100%** เพราะทุกใบมี registry entry อย่างน้อย 1 hook แม้บางใบจะ skip clause ย่อยบางส่วน (เช่น attack-gate ของ Toguro, meta "Choose1→Choose2" ของ Koenma, on-win-fetch ของ Spirit Sword, raid-from-Outside-Area ของ Spirit Reflection Blast, next-event-AP-discount ของ Yukina — ทั้งหมดยังคง undone ตาม convention เดิมของ session นี้) — **บทเรียนรอบนี้**: การ์ดที่มีหลาย clause ผสมกัน ถ้า implement ได้บาง clause ก็ยังนับ "covered" เต็มใบตามเกณฑ์วัดของโปรเจค (มี registry entry = covered) — ไม่จำเป็นต้อง implement ครบ 100% ของทุก clause ในการ์ดเดียวกันเพื่อให้ series แตะ 100%
- **engine hook รอบ HIQ (Haikyu!!)**: เพิ่ม `Effects.registry[no].canAttack(p,unit) -> boolean` hook ใหม่ (conditional attack eligibility ประเมินสด ต่างจาก `kw.cannotAttack` แบบถาวร — เช็คทั้งใน `attackPhase` **และ** bot.js's `chooseAttacker` filter พร้อมกันเสมอ ไม่งั้น bot จะวนลูปไม่รู้จบเหมือนบั๊กเดิมของ `cannotAttack` จาก TSK), `p._grantedRaidDraw` (grant ชั่วคราว "เมื่อ character ของคุณ raid แล้วจั่ว 1 ใบ" — เช็คใน `raidCard` หลัง raid สำเร็จ, reset ทุก `startPhase`), ขยาย `RX_SELF_GEN_RETIRE`/`RX_SELF_GEN_RETIRE2`/"generates N additional" ทั้ง 3 ตัวให้รองรับ "This Field" ไม่ใช่แค่ "This character" (ปลดล็อค 3 ใบ Field ในซีรีส์นี้ทันทีก่อน script อะไรเลย — บทเรียนเดิมซ้ำอีกครั้ง) ได้ 100/102 (98%) เหลือ skip 2 ใบ: manual-trigger-Raid mechanic (ค้นหาการ์ด raid ที่ตรงเงื่อนไขในมือ), "onAnyLeaveField" watcher hook ที่ยังไม่มี (มีแค่ onAnyAttack/onAnyWinBattle/onAnyLoseBattle) — ย้อนกลับไปอัพเกรด `UAPR-YYH-P-001` (Toguro) ให้ใช้ `canAttack` hook ใหม่นี้ด้วย (เดิม skip ไว้ตอนทำ YYH เพราะยังไม่มี hook) — **บทเรียนรอบนี้**: เมื่อเจอ mechanic ใหม่ที่ต้องสร้าง engine hook ให้เช็คว่าการ์ดใบก่อนๆ ที่เคย skip ด้วยเหตุผลเดียวกันหรือไม่ ถ้าใช่ให้ย้อนกลับไปแก้พร้อมกันในรอบนี้เลย ไม่ต้องรอ
- **engine hook รอบ TLR (To Love-Ru)**: ไม่มี engine hook ใหม่ แต่เจอ **data-quality bug ใหม่**: 6 ใบ (`UAPB-TLR-1-*`, ทั้งหมด `main:true`) มี effect text เป็นแค่เลขหลุด "-N" ที่มาจาก field อื่นตอน scrape ไม่มีข้อความเกมจริงเลย (ตรวจแล้วพบอีก 25 ใบทั่วฐานข้อมูลที่เป็นแบบเดียวกันแต่ `main:false` อยู่แล้วเลยไม่กระทบผลรวม) — แก้ที่ชั้นวัด coverage โดยเพิ่มเงื่อนไข `!/^-?[0-9]+$/.test(c.effect.trim())` เข้าไปในตัวกรอง "การ์ดที่มี effect" ทั้ง 3 tool files (ลดฐานรวมจาก 6,264 เหลือ 6,258 อย่างถูกต้อง ไม่ใช่การเลี่ยงนับ) เพิ่ม generic bpEvaluator 2 เงื่อนไขใหม่: "if there is a character on your opponent's area with BP N or more, +M BP" (`cond.oppBpMin`) และ "if there are N or more OTHER characters on your area, +M BP" แบบไม่มีชื่อ/trait กำกับ (`cond.bareOtherCount` — ต่างจาก `countMatching` เดิมที่ต้องมี name/trait เสมอ) ปลดล็อค coverage ได้หลายใบก่อน script อะไรเลย ได้ **76/76 (100%)** — series ที่ 2 ที่ทำได้ครบ 100%
- **engine hook รอบ GIM (Girls und Panzer)**: series ใหญ่และเน้น combo ระหว่างตัวละครหนักมาก (Misuzu Hataya/Ume Hanami/Temari Tsukimura/Kotone Fujita/Saki Hanami web of cross-references) เพิ่ม hook ใหม่ 2 ตัว: `Effects.onAnyPlay(G,p,playedUnit,selfUnit)` (broadcast ไปยังทุกยูนิตของผู้เล่นเมื่อมีการ์ดใดถูกเล่น — wired ครั้งเดียวกลางใน common.js's `Effects.onPlay` wrapper ผ่าน `dispatchOnPlay()` ที่แยกออกมา ไม่ต้องแก้ 4 call sites ใน engine.js), `Effects.onAnyUnblockedAttack(G,p,atkUnit,selfUnit)` (broadcast เมื่อการโจมตีไม่ถูก block และไม่มี snipe target เลย — เติม "unblocked attack detection" gap ที่เจอซ้ำมาตั้งแต่ GMR) — ใช้ hook ใหม่ทั้ง 2 ตัวคู่กับ `onWinBattle`/`onAnyLoseBattle` ที่มีอยู่แล้วเพื่อ implement "at the end of THIS character's attack (ไม่ว่าผลจะเป็นอย่างไร)" ได้สำเร็จ (Kotone Fujita EX13-2-031) — **การค้นพบสำคัญรอบนี้**: พบว่า engine มี `p.pendingDiscount` primitive อยู่แล้ว (เคยสร้างไว้ตั้งแต่รอบก่อนๆ แต่ไม่เคยถูกใช้) สำหรับ mechanic "ลด AP/energy cost ของการ์ดใบถัดไปที่เข้าเงื่อนไข" — เอามาใช้กับ Hiro Shinosawa/Come On Let's Hold It Together โดยตรง ไม่ต้องสร้าง infra ใหม่เลย (ย้อนไปดู YYH's Yukina-090 ที่เคย skip ไว้ด้วยเหตุผลเดียวกัน แต่รอบนี้ไม่ได้ย้อนไปแก้เพราะเวลาจำกัด) ได้ 139/141 (99%) เหลือ skip 2 ใบ: end-of-Attack-Phase hook เดิม, manual-trigger-Raid mechanic เดิม — **บทเรียนรอบนี้**: ก่อนเพิ่ม engine hook ใหม่ ให้เช็คโค้ดที่มีอยู่แล้วก่อนเสมอว่ามี primitive ที่ยังไม่ได้ใช้ซ่อนอยู่หรือไม่ (`p.pendingDiscount` เป็นตัวอย่างที่ชัดเจน)
- **engine hook รอบ SNF (Shangri-La Frontier)**: เพิ่ม generic pattern ใหม่ `matchAllOwnBuff` ("[On Play] All characters on your area get +N BP" แบบไม่มีเลือกเป้าหมาย), `p._cannotUseEventsThisTurn` (บล็อกฝ่ายตรงข้ามใช้ Event Card จากมือเทิร์นนี้ — เช็คใน `playEvent`), `p._eventNeedsPlayedThisTurn` (ค่า required-energy สูงสุดของ Event Card ที่เล่นเทิร์นนี้ — track ใน `trackPlayedTraits` เดียวกับที่ track AP cost อยู่แล้ว) ได้ **78/78 (100%) — series ที่ 3 ที่ทำได้ครบ** เหลือ skip clause ย่อยบางส่วนในการ์ดที่ implement บางส่วนแล้ว (reactive ต่อการ์ดเฉพาะใน Outside Area, "treated as Event Card" type-reclassification แบบกว้าง, meta BP-range-increase effect, forced-block grant — ทั้งหมดเป็น gap ที่เจอซ้ำจากรอบก่อนๆ)
- **engine hook รอบ SYN (Synduality Noir)**: เพิ่ม `p._returnedToHandThisTurn` (นับจำนวนครั้งที่การ์ดถูกส่งกลับมือเทิร์นนี้ — เซ็ตใน `returnUnitToHand`), `unit._playedFromDeck` + `p._playedFromDeckThisTurn` (เซ็ตใน `playCardFromZone` เมื่อ zone==='deck' — สำหรับการ์ดที่เช็คว่าตัวเองหรือการ์ดอื่นถูกเล่นจากเด็คเทิร์นนี้หรือไม่), local player-level flag `p._mirrorOpponentDrawThisTurn` (เฉพาะไฟล์นี้ — mechanic "ถ้าฝ่ายตรงข้ามจะจั่วจาก effect ของคุณ ให้คุณจั่วแทน" เช็คเฉพาะการ์ดที่ทำให้ฝ่ายตรงข้ามจั่วในไฟล์เดียวกัน) ได้ 99/100 (99%) เหลือ skip 1 ใบ: next-ability-activation cost discount ("next [Main] loses [Pay 1 AP]") ต่างจาก `p.pendingDiscount` ที่ครอบคลุมแค่ cost ตอนเล่นการ์ด ไม่ใช่ cost ตอนใช้ ability — **บทเรียนรอบนี้**: พบว่า `unit.tempDmg`'s truthy-OR-fallback formula (`atk.tempDmg || atk.kw.dmg || 1`) ไม่สามารถแทนค่า "บังคับเป็นศูนย์" ได้ (เพราะ 0 เป็น falsy จะ fallback กลับไปที่ kw.dmg เสมอ) — เจอตอนพยายาม implement "this attack deals no damage" ของ UAPR-SYN-P-002 เลยต้อง skip clause นั้นแทนที่จะแก้ sentinel ซึ่งเสี่ยงกระทบการ์ดเก่าหลายใบที่ใช้ `tempDmg += 1` แบบ additive อยู่แล้ว
- **engine hook รอบ NGR**: ขยาย `kw.nullifyImpact` ให้รองรับ typo ที่พบซ้ำ "Impact Nagate" (แทน "Impact Negate") ปลดล็อค 1 ใบทันทีก่อน script อะไรเลย ได้ 71/74 (96%) เหลือ skip 3 ใบ: "changed from active to rest by your effect" tracker ที่ไม่มี (ต้องแก้ script เก่าหลายใบทั่วโปรเจคที่ set `.rested=true` ตรงๆ ไม่คุ้ม), "[Main][When in Outside Area]" activation mechanic เดิม (gap ซ้ำจาก KIN-1-079)
- **engine hook รอบ TRK (Toriko)**: เพิ่ม `kw.retireToRemoval` (static "if this card is retired, it goes to the Remove Area instead" — เช็คใน `sidelineUnit` ก่อน push เข้า sideline/removal), `unit._playedFromSideline` (คู่กับ `_playedFromDeck` เดิม — เซ็ตใน `playCardFromZone` เมื่อ zone==='sideline'), `unit.tempCannotAttack`/`tempCannotBlock` (temp คู่หูของ `kw.cannotAttack`/`kw.cannotBlock` ถาวร — เช็คคู่กับ bot.js's `chooseAttacker` filter เหมือน `tempCannotMove`), local helper `totalTreatedNeed()` ใน `trk.js` (รองรับ "This card is also treated as having required energy of N" ที่ใช้กับ combo check "total required energy ≥20" เท่านั้น ไม่กระทบ cost จริง) ได้ 106/107 (99%) เหลือ skip 1 ใบ: targeting-tax mechanic เดิม (gap ซ้ำจาก TSK/OPM/HIQ)
- **engine hook รอบ NIK (Goddess of Victory: NIKKE)**: series ใหญ่ (158 ใบ) ยอมรับ skip มากกว่าปกติเหมือน KMR เพิ่ม local helper `restOwnFrontActive()`/`frontRestedByEffectThisTurn()`/`plantTopFacedown()` ใน `nik.js` (mechanic "รางวางนอน character ตัวเองผ่านเอฟเฟกต์ตัวเอง" ที่วนซ้ำ 4 ใบในซีรีส์นี้ — Viper/Jackal ใช้ marker `unit._restedByEffectTurn` ร่วมกัน), ใช้ `onAnyUnblockedAttack` hook ใหม่จาก GIM round กับการ์ดที่มี "attacks and is not blocked, draw 1" หลายใบ (Julia, Blue Ocean) ได้ 154/158 (97%) เหลือ skip 4 ใบ: Life-card-trigger-resolution reactive (Dorothy/Noah, ไม่มี hook สำหรับ "การ์ดที่เปิดจาก Life แล้วไม่มี Trigger ถูกส่งไป Outside Area"), end-of-Attack-Phase hook เดิม, hand-level reactive เดิม
- **engine hook รอบ BCV (Black Clover)**: ขยาย `kw.entersActive` ให้รองรับ typo "This field is play in active" (แทน "is played as active") ปลดล็อค 1 ใบก่อน script อะไรเลย ได้ 99/100 (99%) เหลือ skip 1 ใบ: "watch ฝ่ายตรงข้ามใช้ [Main]" reactive ใหม่ (ไม่มี hook สำหรับสังเกตการใช้ ability ของฝ่ายตรงข้าม)
- **engine hook รอบ RNK (Rurouni Kenshin)**: ขยาย `kw.cannotEnterFront` ให้รองรับ "cannot be played to Front Line" แบบไม่มี "the" ปลดล็อค 1 ใบก่อน script อะไรเลย ตัวอย่างใหม่ของการใช้ `onWinBattle` return `true` เพื่อ override ผล battle ปกติทั้งหมด (Kenshin Himura-087: ย้ายศัตรูที่แพ้ไป Energy Line แทนการ retire) ได้ 107/109 (98%) เหลือ skip 2 ใบ: replacement-effect ที่ไม่มี hook (ป้องกันการ์ดถูกเอฟเฟกต์ฝ่ายตรงข้ามนำออกจากสนาม), mill-from-deck reactive ใหม่ (ไม่มี hook สำหรับติดตามว่าการ์ดใบไหนถูก mill ออกจากเด็ค)
- **engine hook รอบ TOA (Tales of Arise)**: เพิ่ม `Effects.registry[no].canAttack`-เทียบเท่าใหม่คือ `unit._grantedUnblockedDraw` (temp คู่หูของ `_grantedAttackDraw` เดิม — "[1 Per Turn] when this character attacks and is not blocked, draw 1" เช็คในจุดเดียวกับ `onAnyUnblockedAttack` broadcast), แก้ regex 3 จุด: `textApDelta`/`hasTextCostDiscount` เพิ่มรองรับ "If there is a &lt;NAME&gt; on your Front Line, in your hand, this card's AP Cost is reduced by -N" (คนละ pattern จาก "reduce the AP cost..." เดิม), `textNeedDelta`/`hasTextCostDiscount` เพิ่มรองรับ "in your hand," แทรกก่อน "reduce" (คำสั่งสลับตำแหน่ง), และ **บั๊กสำคัญที่พบ**: `RX_SELF_GEN_RETIRE` เคยพังกับข้อความที่มีช่องว่างซ้อน/หายไปรอบ `[green]`/`[red]` bracket-tag (เช่น "energy [green]  during" สองช่องว่าง หรือ "[red]during" ไม่มีช่องว่างเลย) เพราะ regex ใช้ literal single-space คั่นระหว่าง optional bracket-group กับคำต่อไป — แก้เป็น `\s*` แทน ปลดล็อค coverage ทั่วทั้งฐานข้อมูลทันทีที่แก้ครั้งเดียว (ไม่ใช่แค่ TOA) ได้ **106/106 (100%) — series ที่ 4 ที่ทำได้ครบ** — **บทเรียนรอบนี้**: ระวัง regex ที่ใช้ literal space คั่นระหว่างส่วนที่ optional กับส่วนถัดไป เพราะข้อมูลสะกดจริงมีทั้งช่องว่างซ้อนและช่องว่างหายไปพร้อมกันในฐานข้อมูลเดียวกัน ใช้ `\s*`/`\s+` แทน literal space เสมอในจุดต่อของ pattern ที่มี optional group นำหน้า
- **engine hook รอบ KJN (The Eminence in Shadow)**: mechanic หลักของ series คือ "วางการ์ดบนสุดของเด็คไป Remove Area แล้วใช้การ์ดนั้นได้ภายในเทิร์นนี้" — ไม่มี "ช่วงเวลาหลังบ้าน" ให้ตัดสินใจทีหลังในเทิร์นเดียวกันของ engine ปัจจุบัน จึงย่อให้เป็น "เสนอให้ใช้ทันที" แทน (`millToRemovalAndOfferPlay()` ใน `kjn.js` — `playCardFromZone` รองรับ zone `'removal'` อยู่แล้วโดยไม่ต้องแก้ engine), local helper `countDistinctTrait()`, ตัวอย่างใหม่ของ "ฝังการ์ดตัวเองทั้งใบ" ผ่าน `unit.counters` (Aurora — ย้ายจาก field ไปเป็น counter ของการ์ดอื่น) ได้ 74/75 (99%) เหลือ skip 1 ใบ: next-ability-activation cost discount ที่ผูกกับ mechanic "ใช้ทีหลัง" ที่ถูกย่อไปแล้ว ทำให้ไม่มีจังหวะให้ discount นี้ apply
- **engine hook รอบ GNT (Gintama)**: ไม่มี engine hook ใหม่ แต่เพิ่ม generic pattern ใหม่ใน `matchOnMain`: "For this turn this character generate +N [color] Energy. During end of main phase, retire this card." (สำนวนสะกดต่างจาก `RX_SELF_GEN_RETIRE`/`RX_SELF_GEN_RETIRE2` เดิมทั้งคู่ ปลดล็อค 3 ใบทันทีก่อน script อะไรเลย) ได้ 96/101 (95%) เหลือ skip 5 ใบ: "onAnyReturnToHand" broadcast hook ที่ไม่มี (มีแค่ onAnyPlay/onAnyAttack/onAnyWinBattle/onAnyLoseBattle/onAnyUnblockedAttack), replacement-effect ป้องกัน [On Retire] ของการ์ดอื่น (gap ซ้ำจาก RNK's Shikijo), end-of-Attack-Phase hook เดิม, hand-level reactive เดิม, raid-target-grant mechanic ใหม่ (การ์ดให้ตัวเองเป็นเป้า raid ได้สำหรับ raider ตระกูลหนึ่งโดยไม่ตรง [Raid] ที่ราider พิมพ์ไว้ — ต้องแก้ `raidTargetsFor` ไม่คุ้มสำหรับ 1 ใบ) — **บทเรียนรอบนี้**: พบบั๊กจากรอบก่อน (KIN/OPM's "play from deck" helpers) ซ้ำอีกครั้งใน `gnt.js` เอง — ลืม push การ์ดกลับ `p.deck` ก่อนเรียก `Engine.playCardFromZone(p, no, 'deck', ...)` ทำให้ effect เงียบไม่ทำงาน แก้ด้วย `p.deck.unshift(no)` ก่อนเรียกเสมอ เป็นรูปแบบบั๊กที่เกิดซ้ำได้ง่ายเวลาดึงการ์ดออกจาก array ชั่วคราวแล้วพยายามเล่นมันจาก zone เดิม
- **engine hook รอบ MST (Mushoku Tensei)**: ไม่มี engine hook ใหม่ — local player-level flag `p._retiredFieldTurn` (เฉพาะไฟล์นี้ เซ็ตโดยการ์ดตัวเองที่ retire Field ของตัวเอง — ใช้กับ Talhand-046's passive "if you retired a Field this turn, +1 gen") ได้ **74/74 (100%) — series ที่ 5 ที่ทำได้ครบ** เหลือ skip clause ย่อยในบางใบตามเดิม (targeting-tax/end-of-Attack-Phase/hand-level reactive/"[Main][When in Outside Area]" self-reactivation/"or raid it" alternative — ทั้งหมด gap ซ้ำจากรอบก่อนๆ, และ static "cannot be played/moved to Energy Line, cannot stand except by own effects" ของ Rudeus-073 ที่ implement เฉพาะ onMain ability ไม่ implement ข้อจำกัดสถิต) — **จับบั๊ก play-from-sideline ซ้ำก่อน commit**: พบตัวเองพลาดแพทเทิร์นเดิม (KIN/OPM/GNT's "pre-splice แล้วเรียก playCardFromZone ด้วย zone เดิม") 3 จุดในไฟล์นี้เอง (039/074/076 ทั้งหมด splice ออกจาก `p.sideline` ก่อนแล้วส่ง zone `'sideline'` เข้า `playCardFromZone` ทำให้ internal `indexOf` หาไม่เจอ) — แก้โดยอ่าน `p.sideline[idx]` เฉยๆ ไม่ splice ก่อนเรียก ปล่อยให้ `playCardFromZone` splice เอง ตรวจพบตอน code-review ก่อนรัน test ไม่ใช่ตอน debug ทีหลัง (เร็วกว่ามาก)
- **engine hook รอบ SHY (The Dangers in My Heart / "Shy")**: ไม่มี engine hook ใหม่ — ใช้ combo `onWinBattle`(return false)+`onAnyLoseBattle`+`onAnyUnblockedAttack` จาก GIM round ซ้ำอีกครั้งกับ 2 ใบที่มี "at the end of this character's attack, ..." (Shy-050: ย้ายตัวเองไป Energy Line แบบถาวรทุกครั้ง; Shy-051: กลับมือ แบบ grant ชั่วคราว "เทิร์นนี้เท่านั้น" ใช้ marker `unit._returnOnAttackEndTurn = Engine.G.turn` แทนการพึ่ง engine-level reset เพื่อ scope ให้ตรงเทิร์นที่ grant จริง — ป้องกัน grant ค้างข้ามเทิร์นถ้าตัวละครไม่ได้โจมตีเทิร์นที่ grant), ใช้ `unit.skipNextStand` เดิมจาก ARK round กับ Shy-041, local helper `sidelineToDeckTop()`/`sidelineToDeckBottom()`/`buffNamedOwn()` ใน `shy.js` เอง, ตัวอย่างการบังคับฝ่ายตรงข้ามเลือกการ์ดจาก Outside Area ของตัวเองผ่าน `enemy.controller.chooseCardFromSideline(enemy, ...)` (ยืนยัน pattern เดิมจาก kmy.js) ได้ 76/78 (97%) เหลือ skip 2 ใบ: Life-card-trigger-resolution reactive เดิม (gap ซ้ำจาก NIK's Dorothy/Noah — "เมื่อการ์ด [Draw] ถูกวางจาก Life ไป Outside Area")
- **engine hook รอบ IYS (Inuyasha)**: เพิ่ม `p._playedFromSidelineThisTurn` (คู่หูของ `_playedFromDeckThisTurn` เดิม — เซ็ตใน `playCardFromZone` เมื่อ zone==='sideline' — สำหรับ "[When Attacking] if a character was played from your Outside Area during this turn, ..."), ใช้ `onAttackPhaseEnd` hook ใหม่จาก TKN round ซ้ำทันที (Koga-054: grant ชั่วคราว "จบ Attack Phase แล้วกลับมือถ้า BP<=4000" ผูกกับ turn-scoped guard `unit._returnIfWeakTurn`) ได้ **75/75 (100%) — series ที่ 12 ที่ทำได้ครบ**
- **engine hook รอบ BAC (Blue Archive)**: ไม่มี engine hook ใหม่ — ขยาย `kw.cannotEnterFront` regex ให้รองรับสำนวน "This card can only be played on...the Front Line using your effects." (Shun-057 — implies การเล่นจากมือปกติถูกบล็อค แต่ `playCardFromZone` ที่ใช้โดยการ์ด "ลงด้วย effect" ไม่ได้เช็ค `cannotEnterFront` อยู่แล้วเลยยังทำงานถูกต้อง) ปลดล็อคก่อน script อะไรเลย เป็น database-wide generic fix อีกครั้ง, local helper `unraidTopLayerToHand()` ใน `bac.js` เอง (คู่หูของ `H.unraidTopLayer`/`unraidTopLayer`-to-hand variant ที่เคยเจอใน rly.js — ยกเลิก Raid State แล้วส่งการ์ดที่เผยออกมากลับมือแทน Outside Area) ได้ 103/104 (99%) เหลือ skip 1 ใบ: "activate/react จาก Outside Area" เดิม (Ayane-101, gap เดียวกับ KIN/NGR/TKG)
- **engine hook รอบ CGD (Code Geass R2)**: ไม่มี engine hook ใหม่ — ใช้ `onAnyLeaveField` (จาก CSM round) กับ Christoph Scissorman-055's passive "if a character on your area was retired this turn, +1000 BP" (ประมาณด้วย "left the field" รวมทั้ง retire และ return-to-hand ตามธรรมชาติของ hook นี้), local helper `lookTopPlayFromDeck()` ใน `cgd.js` เอง (เผยการ์ดบนสุด N ใบ เลือกลงสนามได้ทันทีแบบ rested แทนที่จะเข้ามือ — ใช้กับ "Nara's Negotiation Skills"-079 และ "Aircraft Provide"-037 ลอกจาก Outside Area แทน) ได้ **74/74 (100%) — series ที่ 13 ที่ทำได้ครบ** (ไม่มีใบ skip เลย — ทุกใบมี registry entry แม้บางใบจะ implement แค่บาง clause เช่น "or raid it" alternative ที่ข้ามไปทั้ง 037/079)
- **engine hook รอบ DST (Dr. STONE)**: เพิ่ม generic pattern ใหม่ใน `matchOnMain`: "[Main][Rest this card] Draw N card(s), place N2 card(s) from your hand to the Outside Area." (ปลดล็อค 10 ใบทั่วฐานข้อมูลทันทีก่อน script อะไรเลย รวม Ishigami Senku-106 ในซีรีส์นี้เอง) local helper `declareAndPeekHand()` ใน `dst.js` เอง (mechanic ใหม่ "ประกาศเลข required energy แล้วให้ฝ่ายตรงข้ามเลือกการ์ดจากมือของคุณมาเปิดเผย" — เรียก `enemy.controller.chooseCardFromHand(p, ...)` ข้ามฝั่งได้เพราะ helper รับ `p` เป็นพารามิเตอร์อยู่แล้วไม่ผูกกับ controller เจ้าของ), `forceToRemoval()` local copy (จาก ims.js), `unit.counters` แบบ "วางการ์ดบนสุดของเด็คคว่ำใต้ตัวเอง" (Ogawa Yuzuriha 073/104) ได้ 100/103 (97%) เหลือ skip 3 ใบ: "revealed in hand by your own effect" reactive ใหม่ (Kohaku-045/Magma-046, ไม่มี hook ระดับมือสำหรับ reveal), "changed active→rest by your own effect" tracker เดิม (Kaseki-081)
- **engine hook รอบ MMM (Puella Magi Madoka Magica)**: เพิ่ม `unit._playedFromRemoval` (คู่หูของ `_playedFromDeck`/`_playedFromSideline` เดิม — เซ็ตใน `playCardFromZone` เมื่อ zone==='removal' — สำหรับ "[On Play] if this character was played from the Remove Area, ..." — Sayaka Miki-024), local helper `forceToRemoval()` (จาก ims.js), ใช้ `H.lookTopAndDiscard(p,n,n,title)` (maxDiscard=n เต็มจำนวน) รองรับสำนวน "keep any number on top, remainder to Outside Area" ได้พอดีโดยไม่ต้อง helper ใหม่ (Madoka Kaname-048) ได้ **103/103 (100%) — series ที่ 14 ที่ทำได้ครบ** (ไม่มีใบ skip เลย — ทุกใบมี registry entry แม้บางใบจะ implement แค่บาง clause เช่น "Law of Cycles"-028's event-self-placement ที่ข้ามไป)
- **engine hook รอบ MCR (Macross)**: series ที่ทำค้างมาตั้งแต่ต้นโปรเจค (178 ใบ, uncovered 33 ใบ) ไม่มี engine hook ใหม่ — เพิ่ม registry entries โดยตรงใน `js/effects/mcr.js` เดิม (ไฟล์แรกสุดของโปรเจค ไม่ใช่ไฟล์ใหม่) ใช้ `onAttackPhaseEnd` เดิมกับ VF-19EF/A Isamu Special-011 ("at the end of Attack Phase, draw, move self to bottom of deck, swap Energy→Front"), `H.unraidTopLayer`/`tempCannotAttack` เดิมกับ GIRAFFE BLUES-061, `_grantedUnblockedDraw`/`_grantedOnWinDraw` เดิมกับ Sheryl Nome-051/Ranka Lee-052 (ยอมรับข้าม "cannot repeat the same effect as other same-name cards this turn" exclusivity clause), local helper `distinctNamedInSideline()` ได้ 174/178 (98%) เหลือ skip 4 ใบ: raid-chain mechanic ใหม่ 2 ใบ (Sheryl Nome-008/Isamu Dyson-041 — "ยกเลิก Raid แล้ว raid ใหม่ทันทีด้วยเงื่อนไข", ซับซ้อนเกินคุ้มสำหรับ 2 ใบ), damage-reduction mechanic ใหม่ (Mikumo Guynemer-042, ไม่มี hook ลดความเสียหายขาเข้า), reactive ต่อ "raid-source cards ที่หลุดออกมาตอนการ์ดใน Raid State ออกจากสนาม" (Liza Hoilie-073, ไม่มี hook)
- **engine hook รอบ EVA (Evangelion)**: ไม่มี engine hook ใหม่ — เพิ่ม registry entries ใน `js/effects/eva.js` เดิม ใช้ `onAnyPlay` เดิมกับ Rei Ayanami-003's passive "if a [Get]-trigger character played this turn", `unit.counters` แบบ "วางการ์ดบนสุดของเด็คคว่ำใต้ตัวเอง" กับ Rei Ayanami (Tentative Name)-026 (bpBonus คำนวณจากจำนวน counters ตรงๆ, `onTurnStart` เช็ค retire ถ้า counters>=3), `_grantedUnblockedDraw` เดิมกับ Mari Makinami Illustrious-056 ได้ 106/108 (98%) เหลือ skip 2 ใบ: trigger-interception mechanic ใหม่ (Misato Katsuragi-008, ไม่มี hook redirect ผล trigger), Life-card-trigger-resolution reactive เดิม (Asuka Langley Shikinami-087)
- **engine hook รอบ SMD (Sakamoto Days, 2nd pass)**: series นี้เคย skip ไปเกือบทั้งหมดตั้งแต่รอบแรก (เหตุผลหลัก: mechanic "face-up card ใน deck/Life" ที่ตัดสินใจข้ามทั้ง series) รอบนี้เพิ่ม `Engine.resolveTrigger` เข้า export list (มีอยู่แล้วในเครื่องยนต์แต่ไม่เคยถูก expose — สำหรับการ์ดที่ "activate the Trigger effect" ของ character บนสนามตรงๆ โดยไม่ผ่านการต่อสู้) ใช้กับ Taro Sakamoto-050 (ข้าม [Get]/[Raid] trigger type เพราะ `resolveTrigger` สมมติว่าการ์ดต้นทางถูกใช้จาก Life แล้วหายไป ถ้าเรียกกับการ์ดที่ยังอยู่บนสนามจะเกิดบั๊กการ์ดซ้ำ) ได้ 71/77 (92%) เหลือ skip 6 ใบ: face-up mechanic เดิม (3 ใบ), trigger-override mechanic ใหม่ (Nagumo-060, คล้าย EVA's 018), raid-with-trigger-effect mechanic ใหม่ (Nagumo-058), imprecise "enemy Front Line retired this turn" tracker (Sakamoto's Family Rules-036, ไม่มี hook แยกฝั่ง/แยก line)
- **engine hook รอบ KMR (Kamen Rider, 2nd pass)**: เพิ่ม hook ใหม่ **`Effects.onBeingBlocked(G,p,atkUnit,blockerUnit)`** (fires บน registry ของ**ผู้โจมตี**ทันทีที่ถูกประกาศ blocker — ต่างจาก `onBlock` ที่ fire บนฝั่งบล็อกเกอร์ — ปิด gap "attacker reacts to being blocked" ที่พบ 3 ใบพร้อมกันในรอบนี้: Kamen Rider Fourze Base States-033/Kamen Rider W HeatMetal-054/Kamen Rider Faiz-083), ใช้ `_playedFromRemoval` เดิมกับ Kamen Rider Nadeshiko-028 ("when a Fourze-named character is played from Remove Area"), `onAttackPhaseEnd` เดิมกับ Powerdizer-040/Faiz Axel Form-085, `frontGenBonus` hook เดิมกับ Kamen Rider Agito (DCD)-070, raid-from-hand ทำเองตรงๆ ใน registry (ไม่ผ่าน `raidCard` เพราะไม่ได้ export — ต้อง dump `target.counters` และเรียก `Effects.onRaided`/`Effects.onPlay` เองให้ครบตาม pattern ของ engine) กับ Kamen Rider Den-O Climax Form-076 ได้ 197/202 (97%) เหลือ skip 5 ใบ: reactive-to-Remove-Area-by-own-effect เดิม (2 ใบ), ability-borrowing เดิม (Kamen Rider Decade-067), targeting-tax เดิม (Kamen Rider Kabuto-071), compound multi-clause ที่ซับซ้อนเกินคุ้มสำหรับ 1 ใบ (Kamen Rider Fourze Rocket States-035)
- **engine hook รอบ KMY (Kimetsu no Yaiba, 2nd pass)**: ไม่มี engine hook ใหม่ — ใช้ `unit._movedThisTurn` เดิม (ประมาณจาก "moved this turn" ทั้งเทิร์น ไม่ใช่แค่ระหว่าง Attack Phase — Kamado Tanjiro (2)-001), `onAttackPhaseStart` เดิมกับ Kamado Nezuko-002 (สลับตำแหน่งกับ character บน Front Line ตอนเริ่ม Attack Phase) ได้ 176/184 (96%) เหลือ skip 8 ใบ (เพิ่มจาก 10 เดิม): "chosen by Trait:Nichirin Sword card effect" tracking ใหม่ (3 ใบ, ต้องแก้การ์ด Nichirin Sword หลายใบทั่ว series เพื่อ set marker — ไม่คุ้มสำหรับผลกระทบวงกว้าง), reactive-to-enemy-character-leaving-field ใหม่ (ไม่มี hook ฝั่งตรงข้าม), reactive-to-self-being-discarded-by-specific-effect ใหม่ (2 ใบ), meta BP-range-increase เดิม, "place self face-up under another same-name Raid character" mechanic ใหม่ (ทิศทาง raid กลับด้าน)
- **engine hook รอบ KGR (Kagurabachi, deep-dive)**: ตรวจสอบ 4 ใบที่เหลือละเอียดแล้วพบว่าทั้งหมดต้องการ mechanic ใหม่ที่ไม่คุ้มสร้าง infra สำหรับ 1-2 ใบ: reactive-to-specific-card-caused-placement (2 ใบ), hand-level phase-start trigger ใหม่ (การ์ดยังอยู่ในมือตอน hook ควรยิง — ไม่มี hook สำหรับการ์ดในมือ), forced-block mechanic เดิม — ไม่มีการแก้ไขรอบนี้ ยังคง 74/78 (95%)
- **engine hook รอบ GNT (Gintama, 2nd pass)**: **พบ measurement bug สำคัญ**: `kw.raidableByAny`/`kw.cannotEnterEnergy`/`kw.cannotMoveToFront` ไม่เคยอยู่ใน `hasKeywordOnly()` checklist ของเครื่องมือวัด coverage ทั้ง 3 ไฟล์ (`uncovered-in-series.mjs`/`coverage-total.mjs`/`coverage-report-full.mjs`) ทำให้การ์ดที่มีแค่ static keyword พวกนี้อย่างเดียว (ไม่มี clause อื่นเลย) ถูกรายงาน "uncovered" ผิดพลาดทั้งที่ mechanic ทำงานถูกต้องในเกมจริงอยู่แล้ว — แก้จุดเดียวได้ **+3 การ์ดทันทีทั้งฐานข้อมูล** (Sadaharu-091 เป็นตัวอย่างที่เจอ) ก่อน script อะไรเลย ใช้ `onAnyLeaveField` เดิมกับ Sasaki Isaburo-003, `onAttackPhaseEnd` เดิมกับ Okada Nizou-043 ได้ 99/101 (98%) เหลือ skip 2 ใบ: replacement-effect เดิม (Sakamoto Tatsuma-036), reactive-to-self-being-discarded เดิม (Shouyou-058) — **บทเรียนรอบนี้ (ก)**: เมื่อเพิ่ม static keyword ใหม่ (`cannotEnterEnergy`/`cannotMoveToFront`/`raidableByAny` ในรอบก่อนๆ) ต้องอัพเดต `hasKeywordOnly()` ในเครื่องมือวัดทั้ง 3 ไฟล์คู่กันเสมอ ไม่งั้นการ์ดที่มีแค่ keyword เดียวจะรายงานผิดตลอดไป
- **engine hook รอบ HTR (Hunter x Hunter, 2nd pass)**: ไม่มี engine hook ใหม่ — **แก้ typo เดียวได้ 3 ใบทันที**: `RX.apActive` regex เขียน `(?:of your )?` แต่ข้อมูลต้นทางสะกด "Choose up to 2 of **you** AP cards" (ตก "r") พบ 7 ใบทั้งฐานข้อมูล แก้เป็น `(?:of your? )?` ตัวเดียวจบ (ยืนยันบทเรียนเดิมอีกครั้ง: เช็ค regex ก่อน script เสมอ), implement Risky Dice-027 (Field [Main] reveal-top แล้วแตกผลตาม required energy ของการ์ดที่เปิด) คู่กับ "Out-!!"-046 ที่ redirect เป้าหมายของ Risky Dice ไปฝั่งศัตรู (player flag `p._riskyDiceTargetsEnemyTurn` — ตัวอย่างการ์ด 2 ใบที่ผูกกันโดยตรง ต้อง implement พร้อมกันถึงจะได้ทั้งคู่), Zushi-068 ("เมื่อ BP ถูกเพิ่ม → +1000 BP") ใช้ `bpBonus` อ่าน `unit.bpMod` ดิบๆ ไม่เรียก `Engine.bp()` จึงไม่เกิด recursion (เดิม skip ไว้เพราะกลัว self-referential loop — วิธีนี้ปลอดภัยเพราะ `bpMod` เป็น field ไม่ใช่ค่าที่คำนวณผ่าน hook) ได้ 177/179 (99%) เหลือ skip 2 ใบ: targeting-tax เดิม (Hisoka-2-024), replacement-effect เดิม (White Goreinu-2-026)
- **engine hook รอบ AND (Angel Beats!, 2nd pass)**: **พบบั๊กใหญ่ที่สุดของ session นี้** — `parseKeywords` จับ `[Snipe]` อย่างเดียว แต่ข้อมูลต้นทางสะกด **`[Sniper]` ถึง 113 ใบ** เทียบกับ `[Snipe]` แค่ 4 ใบ! แปลว่า keyword [Sniper] **ไม่เคยทำงานเลยทั้งเกม**ตั้งแต่ต้นโปรเจค (การ์ดพวกนี้ยังนับ "covered" อยู่แล้วเพราะมี clause อื่น เลยไม่เคยโผล่ใน uncovered list — ซ่อนตัวมาตลอด) แก้เป็น `/\[Sniper?\]/i` จุดเดียว → keyword ทำงานจริง 117 ใบ (**เป็น gameplay-correctness fix ไม่ใช่แค่ coverage** — coverage ขึ้นแค่ 1 ใบแต่ผลต่อการเล่นจริงมหาศาล) เพิ่ม keyword ใหม่ 2 ตัว: **`kw.immuneBpReduction`** ("This character is not affected by BP reducing effects." — clamp ที่ `bp()` จุดเดียวโดยตัด `bpMod`/`bpPersist` ที่ติดลบทิ้ง แทนที่จะไล่แก้ทุกจุดที่เขียน `bpMod -= N` ซึ่งกระจายอยู่หลายสิบไฟล์), **`kw.snipeMaxBP`** ("[Sniper] cannot target characters with BP N or more" — cap เป้าหมายของ snipe ต่างจาก `unblockableBP*` ที่ cap ผู้ block; เช็คทั้งใน attackPhase **และ** bot.js's snipe filter ตาม pattern เดิม) ได้ 71/72 (99%) เหลือ skip 1 ใบ: targeting-tax เดิม (Gina-053) — **บทเรียนรอบนี้**: บั๊ก keyword-parsing แบบนี้ซ่อนตัวได้นานมากเพราะเครื่องมือวัด coverage มองว่าการ์ด "covered" แล้ว (จาก clause อื่น) จึงไม่มีอะไรฟ้อง — คุ้มมากที่จะไล่เช็คเป็นระยะว่า keyword regex แต่ละตัว**แมตช์การ์ดจริงกี่ใบ** เทียบกับจำนวนที่ข้อความการ์ดมีคำนั้นจริงๆ
- **engine hook รอบ TKN (Tekken)**: เพิ่ม hook ใหม่ **`Effects.onAttackPhaseEnd(G,p,unit)`** (broadcast ใหม่ ปิด gap "end of Attack Phase" ที่ยอมรับ skip มาตั้งแต่ GMR — wired ครั้งเดียวหลัง attackPhase loop จบ ก่อน update() สุดท้าย — LILI-052 ใช้ทำ "at end of Attack Phase, swap position with character on another line"), `unit._moveCountThisTurn` counter ใหม่ (คู่หูตัวเลขของ `_movedThisTurn` — สำหรับ "+N BP for EACH TIME moved this turn" ต่างจาก boolean เดิม — เซ็ตทั้งใน Movement Phase และ `moveUnitFree`) ได้ **98/98 (100%) — series ที่ 11 ที่ทำได้ครบ**
- **engine hook รอบ SAO (Sword Art Online)**: เพิ่ม hook ใหม่ **`Effects.onAnyMove(G,p,movedUnit,selfUnit)`** (broadcast ใหม่ — wired ใน `moveUnitFree` โดยตรง ไม่ใช่ Movement Phase ปกติ — สำหรับ "[Your Turn][1/turn] when a character on your area moves outside your Move Phase, ..." ที่พบ 3 ใบในรอบนี้ Asuna-2014/Asuna-1036/Lizbeth-1048), ใช้ `p.pendingDiscount`/`_grantedOnWinDraw`/`tempSnipe` เดิมซ้ำหลายจุด ได้ 168/169 (99%) เหลือ skip 1 ใบ: targeting-tax เดิม
- **engine hook รอบ FMA (Fullmetal Alchemist)**: ขยาย `kw.raidableByAny` regex ให้รองรับสำนวนกลับหัว "Your Character Cards with [Raid] can use this card to raid." (Envy-076, ต่างจาก "you can raid this character..." เดิม) ปลดล็อคก่อน script อะไรเลย, ตัวอย่างใหม่ของการสร้าง Raid State ด้วยมือผ่าน `unit.under` โดยตรง (Alphonse Elric-003: "วางการ์ดหน้าคว่ำใต้ character ที่มี [Raid] เพื่อเข้าสถานะ Raid" — ไม่ผ่าน `raidCard` ปกติ) ได้ 101/102 (99%) เหลือ skip 1 ใบ: targeting-tax เดิม
- **engine hook รอบ TKG (Tokyo Ghoul)**: ไม่มี engine hook ใหม่ ใช้ `onAnyWinBattle` (broadcast ไปทุกยูนิตของ p รวม Field cards ด้วย — Field ก็รับ broadcast ได้เหมือนกันเพราะ loop คือ `[...p.front,...p.energy]` — CCG Headquarters-027 เฝ้าดูว่า "ยูนิตไหนก็ได้ของคุณที่มี Trait:CCG ชนะ battle") ได้ 103/104 (99%) เหลือ skip 1 ใบ: "activate ability จาก Outside Area" เดิม (gap เดียวกับ KIN-1-079/NGR-1-042)
- **engine hook รอบ MSS**: เพิ่ม hook ใหม่ **`Effects.registry[no].canMoveToFront(p,unit) -> boolean`** (คู่หูของ `canPlayFromHand` เดิม — สำหรับสำนวน "cannot be played OR moved to Front Line unless &lt;NAME&gt; present" ที่พบ 3 ใบในซีรีส์นี้ — เช็คทั้งใน Movement Phase และ `moveUnitFree`), global flag ใหม่ **`G._noRemovalByEffectsThisTurn`** (ต่างจาก player-level flags เดิมทั้งหมด — ผูกกับเกมทั้งเกม ไม่ใช่ผู้เล่นคนเดียว เพราะการ์ดให้ผล "ทั้งสองฝ่าย" — เช็คใน `sidelineUnit` เฉพาะ reason==='effect' ไม่ block การแพ้ battle) ได้ 77/78 (99%) เหลือ skip 1 ใบ: replacement-effect เดิม
- **engine hook รอบ MGS (Monogatari Series)**: เพิ่ม `unit._grantedAttackDrawN` (ตัวเลขคู่หูของ `_grantedAttackDraw` เดิม — สำหรับ grant "[When Attacking] draw up to N" ที่ N>1 ไม่ใช่แค่ 1 ใบตายตัว) ได้ 103/106 (97%) เหลือ skip 3 ใบ: reactive-to-milled-by-own-effect กลุ่มใหม่เดิม (2 ใบ, gap เดียวกับ IMS), Event card ทำหน้าที่ raider เอง (Devil's Left Hand — mechanic ใหม่ที่ต่างจาก raid ปกติมากพอจะไม่คุ้มสำหรับ 1 ใบ)
- **engine hook รอบ KJ8 (Kaiju No. 8)**: เพิ่ม `unit.tempCanAttackFromEnergy` (flag ใหม่ — grant "โจมตีจาก Energy Line ได้เทิร์นนี้" ปกติ attacker ต้องอยู่ Front Line เท่านั้น — เช็คคู่กับ `p.energy` ใน attackPhase's attacker lookup **และ** bot.js's `chooseAttacker` candidate list เสมอ mirror pattern ของ permanent "cannot X" keyword ทุกครั้งที่เพิ่ม attack-related state ใหม่) ได้ 71/73 (97%) เหลือ skip 2 ใบ: targeting-tax เดิม (Bakko-023), "changed active→rest by any effect this turn" tracker เดิม (Mina Ashiro-044, gap เดียวกับ NGR/REZ/MHA)
- **engine hook รอบ IMS (Idolmaster)**: series ใหญ่ที่สุดในฐานข้อมูล (219 ใบ, uncovered เริ่มต้น 69 ใบ — รอบใหญ่ที่สุดจนถึงตอนนี้) ยอมรับ skip มากกว่าปกติเหมือน KMR/NIK/MHA เพิ่ม `Engine.dealDamage` เข้า export list (มีอยู่แล้วในเครื่องยนต์แต่ไม่เคยถูก expose ให้ effect script เรียกตรงๆ — สำหรับการ์ด "inflict N damage" ที่ไม่ผ่านการต่อสู้ปกติ), `p._cardsPlayedFromHandThisTurn` counter ใหม่ (เพิ่มใน `trackPlayedTraits` จุดเดียว ครอบคลุมทั้ง playCard/raidCard/playEvent — สำหรับเงื่อนไข "ถ้าคุณใช้การ์ดจากมือ N ใบขึ้นไปเทิร์นนี้" ที่พบซ้ำหลายใบในซีรีส์นี้), `unit._grantedOnWinActive` flag ใหม่ (คู่หูของ `_grantedOnWinDraw` เดิม — เช็คระดับ engine ไม่ใช่ registry เพราะการ์ดที่ถูก grant อาจเป็นชื่อการ์ดไหนก็ได้ ไม่ใช่แค่การ์ดที่ให้ effect เอง) ได้ 212/219 (97%) เหลือ skip 7 ใบ: reactive-to-milled-by-specific-effect กลุ่มใหม่ (6 ใบ — "when this card is placed from deck/to Remove Area by [a specific trait's] effect" ไม่มี hook แยกแยะสาเหตุการย้ายโซนจากการย้ายทั่วไป) และ raid-timing combo เดิม (1 ใบ)
- **engine hook รอบ WBK (Windbreaker)**: ไม่มี engine hook ใหม่ — ใช้ combo `onWinBattle`(return false)+`onAnyLoseBattle` (ไม่ใช้ `onAnyUnblockedAttack` ในรอบนี้เพราะ effect ต้องการเฉพาะ "attacks AND is blocked" ไม่ใช่ unblocked) จับ "attacks and is blocked" ครั้งแรก (Kyotaro Sugishita-064), ตัวอย่างใหม่ "ส่งการ์ดศัตรูกลับเด็คของเจ้าของเอง (บนสุด/ล่างสุด แล้วแต่ใครเลือก)" — `sendEnemyToDeck()` local helper ใน `wbk.js` เอง (mechanic ใหม่ที่ไม่เคยเจอมาก่อน — ปกติการ์ดที่ถูก "retire" ไปที่ sideline/removal เท่านั้น ไม่เคยกลับเด็คของเจ้าของโดยตรง), ใช้ `unit._grantedOnWinDraw`/`_grantedAttackDraw` เดิมซ้ำหลายจุด ได้ **76/76 (100%) — series ที่ 10 ที่ทำได้ครบ** เหลือ skip clause ย่อยเดิม (Hayato Suo-060's "[When in Energy Line] when rested by your character's effect" reactive — ไม่มี hook แยกแยะ "rested by an effect" จาก resting ปกติ)
- **engine hook รอบ MHA (My Hero Academia)**: series ใหญ่และซับซ้อนมาก (171 ใบ) ยอมรับ skip มากกว่าปกติเหมือน KMR/NIK เพิ่ม **`Effects.registry[no].canPlayFromHand(p,card) -> boolean`** hook ใหม่ (ข้อจำกัด "ห้ามเล่นจากมือถ้าเงื่อนไขบนสนามไม่ผ่าน" ประเมินสด เช็คใน `playCard` + filter ออกจาก bot.js's ทั้ง 2 candidate list — คนละแบบจาก `cannotEnterFront`/`cannotEnterEnergy` ที่เป็น zone restriction ถาวร), **`playCardFromZone`'s ตัวเลือกใหม่ `skipOnPlay`** (สำหรับ mechanic หายาก "การ์ดที่ลงด้วย effect นี้ไม่ activate [On Play] ของตัวเอง" — Aoyama Yuga-2050), keyword ใหม่ **`kw.raidableByAny`** (เทียบเท่าถาวรของ `tempRaidable` เดิม — "can raid this character without the required Raided card"), ตัวอย่างการใช้ `onWinBattle` override ร่วมกับ trick "sidelineUnit ปกติแล้วย้ายจาก sideline ไป removal" (Shigaraki Tomura-009: ศัตรูที่แพ้ battle ไป Remove Area แทน Outside Area), ใช้ `onAnyLoseBattle` (Stain-011: บล็อกเกอร์ที่ชนะจะไม่ลุกครั้งถัดไป) ได้ 165/171 (96%) เหลือ skip 6 ใบ: replacement-effect เดิม 3 ใบ (Kirishima-074/Midoriya-093/Shinomori Hikage-2030 — ทั้งหมด "ถ้าจะออกจากสนามโดยเอฟเฟกต์ฝ่ายตรงข้าม ให้ redirect แทน"), meta BP-range-increase เดิม (Bakugo-084), ability-borrowing มากเกินไป (All For One-2038 — ต้อง re-parse ข้อความการ์ดอื่นสด), reactive+replacement ผสมที่ซับซ้อนเกินไปสำหรับ 1 ใบ (Shigaraki-2040)
- **engine hook รอบ BTR (Roboco Channel)**: ขยาย `kw.entersActive` regex ให้รองรับ "Play this field/card on your area and set it to active" (สำนวนที่มี "on your area and" คั่นกลาง ซึ่ง regex เดิมคาดว่าจะต่อกันทันที) ปลดล็อค 2 ใบ (River-061, Gachi Animal Hospital-095) ก่อน script อะไรเลย ใช้ `H.scryTop`/`H.debuffEnemyAny`/`H.bounceEnemyFront` เดิมซ้ำหลายจุด, ตัวอย่างใหม่ของ "ทั้งสองฝ่ายเปิดเผยการ์ดบนสุดของเด็คตัวเองมาเทียบกัน" (`dualRevealToBottom()` local helper ใน `btr.js`), ตัวอย่าง "ฝ่ายตรงข้ามเลือกการ์ดของเราเอง" ผ่าน `enemy.controller.chooseOption(enemy, ...)` (Motsuo-052 — คนละแบบจาก `enemy.controller.chooseCardFromHand` ที่เจอบ่อยกว่า), ใช้ `onBlock(G,p,unit,atkUnit)` hook ครั้งแรกในรอบนี้ (Motsuo-052's "[When Attacking]/[On Block]" dual-trigger) ได้ **107/107 (100%) — series ที่ 9 ที่ทำได้ครบ**
- **engine hook รอบ REZ (Re:Zero)**: ขยาย `kw.alsoTreatedAs` regex ให้รองรับสำนวน "This card is treated as both &lt;NAME1&gt; and &lt;NAME2&gt;." (เดิมจับได้แค่ "also treated as &lt;NAME&gt;" ตัวเดียว) ปลดล็อคการ์ดทันทีก่อน script อะไรเลย และเป็น database-wide fix (เห็นผลใน SAO ด้วย +1 ใบ ไม่ใช่แค่ REZ) ได้ 100/103 (97%) เหลือ skip 3 ใบ: hand-based ability activation ใหม่ (Ricardo Welkin-047 — [Main] ability ที่ activate ได้ตอนการ์ดยังอยู่ในมือ ไม่ใช่บนสนาม ไม่มี UI/hook รองรับการ activate จากมือ), replacement-effect เดิม (Beatrice-074 — ป้องกันการ์ดถูกเอฟเฟกต์ฝ่ายตรงข้ามนำออกจากสนามโดย redirect ไปมือแทน), "changed from active to rest by your effect" tracker เดิม (Emilia-088 — gap เดียวกับที่ยอมรับไว้แล้วตั้งแต่ NGR round)
- **engine hook รอบ KGD (Kingdom)**: เพิ่ม keyword ใหม่ **`kw.cannotMoveToFront`** (ตรงข้ามการเคลื่อนที่ของ `cannotMove` — "cannot be moved to the Front Line" แคบกว่า `cannotMove` เพราะยังย้ายไป Energy Line ได้ปกติ เช็คทั้งใน Movement Phase's move loop และ `moveUnitFree`), ใช้ `Effects.onAttackPhaseStart`/`unit.retireAtEndOfMain` เดิมร่วมกัน (Mou Ten-037: "at start of Attack Phase untap AP" + "retire self ตอนจบ Main Phase" ในใบเดียว), ใช้ `unit.bpPersist` (ไม่ใช่ `bpMod`) ครั้งแรกกับ effect ที่พิมพ์ตรงตัวว่า "until the start of your next turn" (Kankoku Pass-094 — ก่อนหน้านี้ series อื่นมักเจอแต่ "during this turn" ธรรมดา), ตัวอย่างใหม่ของการใช้ `unit._watchers[]` (เดิมมีมาตั้งแต่ ARK round) ผูกกับ turn-scoped guard ในตัว watcher function เอง (`if (u._grantXxxTurn !== Engine.G.turn) return;`) เพื่อให้ grant ชั่วคราว "เทิร์นนี้เท่านั้น" ทำงานถูกต้องแม้ `_watchers` เองไม่มีกลไก auto-expire (Ma Ron-090: grant "[On Retire] กลับมือ" ให้การ์ดอื่น), local helper `retireEnemyFrontMin()` ใน `kgd.js` เอง (targeting แบบ "BP N ขึ้นไป" — ตรงข้ามทิศทางกับ `H.retireEnemyFront`'s upper-bound เดิม) ได้ **108/108 (100%) — series ที่ 8 ที่ทำได้ครบ**
- **engine hook รอบ RLY (Rent-a-Girlfriend)**: เพิ่ม `unit.tempUnblockableNeedMin` (คู่หูของ `tempUnblockableBPMin` เดิม — จำกัดผู้ block ด้วย required-energy แทน BP, "cannot be blocked by characters with required energy N or more" — approximate "until end of the attack" เป็น "จนจบเทิร์น" เหมือน `tempSnipe`), `p._cannotPlayFromHandThisTurn` (flag ใหม่ระดับ player — บล็อกการเล่นการ์ดจากมือ**ทุกชนิด**ทั้งเทิร์น เช็คใน `playCard`/`playEvent`/`raidCard` ทั้ง 3 จุด — กว้างกว่า `_cannotUseEventsThisTurn` เดิมที่จำกัดแค่ Event Card), local helper `unraidTopLayerToHand()` ใน `rly.js` เอง (คู่หูของ `H.unraidTopLayer` เดิมที่ปกติส่งใบบนไป Outside Area — ใบนี้ส่งกลับมือแทน) ได้ **74/74 (100%) — series ที่ 7 ที่ทำได้ครบ** เหลือ skip clause ย่อยเดิม (color-trigger-replacement effect ของ Eiai Nano-052 — ลึกกว่า `onColorTrigger` hook ที่มีรองรับ)
- **engine hook รอบ AOT (Attack on Titan)**: series ใหญ่ที่สุดในบรรดารอบเดี่ยวจนถึงตอนนี้ (169 ใบ, uncovered เริ่มต้น 55 ใบ) เพิ่ม **`Effects.onAttackPhaseStart(G,p,unit)`** hook ใหม่ (broadcast ไปยังทุกยูนิตของ p ที่จุดเริ่ม Attack Phase ของ p เอง — mirror `onTurnStart` เป๊ะๆ แค่ยิงทีหลังในเทิร์นเดียวกัน — ใช้กับทั้ง printed passive "[When in Frontline] at the start of your Attack Phase, if active, ..." **และ** one-shot grant จาก [On Play] ก่อนหน้า เช่น Petra Rall ที่ตั้ง flag ของตัวเองแล้วเช็คใน hook นี้), keyword ใหม่ **`kw.cannotEnterEnergy`** (ตรงข้ามกับ `cannotEnterFront` เดิม — "This card cannot be played on the Energy Line" บังคับใน `playCard` + filter ออกจาก bot.js's Energy-Line-fill candidates, Colossal Titan), local player-level flag `p._paidApAttackPhaseTurn` (เฉพาะไฟล์นี้ — หลายใบมี "[When Attacking] you may pay 1 AP..." แล้วมีใบอื่นเช็คว่า "จ่าย AP ระหว่าง Attack Phase เทิร์นนี้หรือไม่"), ใช้ `unit.enteredTurn` เดิมกับเงื่อนไข "ใช้ได้เฉพาะเทิร์นที่เล่นการ์ดนี้", ใช้ `p.pendingDiscount` เดิมกับ Nicolo, ตัวอย่าง `onLeaveField` โดยตรง (ไม่ใช่ `onAnyLeaveField`) สำหรับการ์ดที่ reactive ต่อการ**คืนตัวเองกลับมือ** (Armin Arlelt-076) ได้ 168/169 (99%) เหลือ skip 1 ใบ: hand-level + End-Phase-timing reactive ผสมกัน (Mikasa Ackerman EX10BT-2-066 — ซับซ้อนกว่า "hand-level reactive" ทั่วไปเพราะต้องมี End-Phase hook ใหม่ด้วย ไม่คุ้มสำหรับ 1 ใบ), และยอมรับ skip 2 จุดที่ต้องแก้ engine กว้างเกินไปสำหรับ 1 ใบ: Colossal Titan's "ลดจำนวนการ์ดที่ลง Front Line ได้ -1" (กระทบทุกจุดที่ hardcode capacity=4), "The Rumbling"'s "grant ห้ามโจมตีด้วย character อื่นนอกจาก X ทั้งเทิร์น" (ต้องมี player-level attack gate ใหม่ทั้ง attackPhase และ bot.js)
- **engine hook รอบ CSM (Chainsaw Man)**: เพิ่ม **`Effects.onAnyLeaveField(G,p,leftUnit,selfUnit)`** (broadcast ใหม่ — ปิด gap "onAnyLeaveField" ที่ตั้งใจไว้ตั้งแต่ HIQ round แต่ไม่เคยสร้างจริง — wired ครั้งเดียวกลางใน common.js's `Effects.onLeaveField` wrapper คู่กับ `onAnyPlay`/`onAnyUnblockedAttack` เดิม ครอบคลุมทั้ง retire **และ** return-to-hand เพราะ engine เดิมเรียก `Effects.onLeaveField` จากทั้ง `sidelineUnit` และ `returnUnitToHand` อยู่แล้ว ไม่ต้องแก้ 2 จุดแยกกัน), `unit._movedByEffectThisTurn` (ตัวเลขคู่หูที่แคบกว่า `_movedThisTurn` เดิม — เซ็ตเฉพาะใน `moveUnitFree` เท่านั้น ไม่ใช่ Movement Phase ปกติ สำหรับเงื่อนไข "moved outside of your Move Phase" ที่ต้องแยกจาก "moved" เฉยๆ), นำ `unit.under.length` (raid-state marker ที่มีอยู่แล้ว) มาใช้เป็นเงื่อนไข "not in raid state" ครั้งแรก, ใช้ `unit._grantedAttackDraw` เดิมกับการ์ดที่ grant ให้การ์ดอื่น (Beam-062), ตัวอย่าง `unit.counters` แบบใหม่ "วางการ์ดจากมือคว่ำใต้ตัวเอง" (Aki Hayakawa-054, แยกจาก convention เดิมที่มักเป็นการ์ดจาก Outside Area) ได้ **75/75 (100%) — series ที่ 6 ที่ทำได้ครบ** — **ยอมรับ simplification 1 จุด**: CSM-076's "วางการ์ด Event ใบนี้เองคว่ำใต้ Denji" ทำไม่ได้เพราะ `playEvent` ส่งการ์ด Event ที่ resolve แล้วไป sideline แบบไม่มีเงื่อนไขทันทีหลัง `onEvent` return เสมอ ไม่มีช่องให้ redirect เข้า `unit.counters` แทนโดยไม่แก้ engine เฉพาะใบเดียว — implement เฉพาะผลลัพธ์ข้างเคียง (ลง Denji ฟรี / AP untap) แทน

### ขั้นตอนทำงานต่อ (ทำตามนี้)
1. `node tools/uncovered-in-series.mjs <SERIES>` — ดูว่า series ไหนเหลือการ์ดที่ยังไม่ครอบคลุม
2. script รายใบลง `js/effects/<series>.js` (ดู mcr.js / eva.js เป็นแบบ; สร้างไฟล์ใหม่ต้องเพิ่ม `<script>` ใน index.html และรายชื่อไฟล์ใน tools/test-engine.mjs, tools/stress-test-all-series.mjs, tools/uncovered-in-series.mjs ด้วย)
3. หา pattern ซ้ำข้าม series → เพิ่มใน `js/effects/common.js` แทนการ script ทีละใบ (`node tools/analyze-patterns.mjs` ช่วยขุด)
4. **ก่อน commit ทุกครั้ง**: `node tools/stress-test-all-series.mjs` ต้องได้ `0 fail` (บอทชนบอท 1 เกม/series ทั้ง 55 series)
5. commit + push ขึ้น main → Pages deploy เอง (~1-2 นาที)

## UI feedback (2026-07-26 — ผู้ใช้ขอ ก่อนไล่ residual ต่อ)

ผู้ใช้รายงานว่า "effect เหมือนไม่ทำงาน" — ตรวจแล้วพบว่า**ส่วนใหญ่ทำงานอยู่แล้ว แต่มองไม่เห็น**:
- `ui.js` เคยตั้ง `Engine.G.onLog = () => {}` → **ทิ้ง log ทุกบรรทัด** และ log panel (`gb-log`) ก็ `hidden` โดย default (เปิดด้วยปุ่ม 📜) — ผู้เล่นเลยไม่เห็นอะไรเลยเวลา effect ยิง
- **แก้แล้ว**: ต่อ `onLog` เข้า `showEventToast()` — feed ลอยกลางบน แสดง log สดทีละบรรทัด (สูงสุด 4 อัน, หายใน 2.6 วิ) · บรรทัดที่เปลี่ยนสถานะกระดาน (Impact/Trigger/BP/retire/บล็อก ฯลฯ) ใช้สไตล์ `.hot` เด่นกว่า
- **badge keyword บนการ์ด** (`keywordBadges()` ใน `unitHtml`): ⚡Impact ✖Damage 🎯Sniper ⚔²DoubleAttack 🛡²DoubleBlock ØImpactNegate ↷Step ⇢unblockable 🔒untargetable ❗mustBeBlocked 🛑mustBlock 🚫⚔/🚫🛡 cannotAttack/Block 💤skipNextStand
  - แสดง**สถานะที่มีผลจริงตอนนี้** = printed keyword + temp/granted + **conditional ที่เงื่อนไขผ่านแล้ว** (เรียก `Effects.genericImpactBonus`/`genericDmgBonus`/`genericKeywordActive`) — ถ้า badge ขึ้น แปลว่า engine จะใช้จริงเทิร์นนี้
  - ตัวที่ได้มาชั่วคราว/จากเงื่อนไข ใช้สี `.granted` (ส้ม) แยกจาก printed (ดำ) เพื่อให้ debug ได้ว่า grant ทำงานไหม
  - ทดสอบผ่าน browser จริง: การ์ดที่มี [Impact 1] printed + grant temp อีก 1 → แสดง `⚡2` และ badge ที่ granted ติดสีส้มถูกตัว · 7 badges กินพื้นที่การ์ด 22% (ปรับ padding/font ลงจาก 42%)

### "Choose 1 of the following" (generic — ผู้ใช้รายงานว่าไม่มีอะไรให้เลือก)
การ์ด 255 ใบพิมพ์เมนูตัวเลือก · 161 ใบมี script รายใบอยู่แล้ว · **94 ใบไม่มีใครถามเลย** clause เงียบสนิท
- เพิ่ม `matchChooseOptions()` + `runChooseMenu()` ใน common.js — อ่าน bullet (`•`/`·`/`・`) ที่ตามหลัง clause "Choose N of the following" แล้ว**เด้งเมนูให้ผู้เล่นเลือกเสมอ โดยแสดงข้อความจริงบนการ์ด** (ต่อเข้า onPlay / onMain / onAttack / onEvent — `matchOnMain` คืน kind `chooseMenu` เพื่อให้ปุ่ม ⚡ ขึ้นด้วย)
- `resolveChosenOption()` แก้ตัวเลือกที่เข้า pattern ให้อัตโนมัติ (self keyword grant, +N BP, set active, draw(+discard), retire/rest/debuff ศัตรู, buff พวกเดียวกัน, AP untap, quoted grant)
- เพิ่ม **conditional unwrapper**: "If your (opponent's) Life is N or less/more, &lt;body&gt;" → เช็ค gate แล้ว **recurse เข้า resolver ตัวเดิม** (ไม่ต้องเขียน pattern ซ้ำต่อเงื่อนไข) — ทดสอบแล้วว่าเคารพเงื่อนไขจริง (Life 3 → ตั้งขึ้น, Life 6 → ข้าม)
- **ผล**: 91/94 ใบมีเมนูแล้ว · auto-resolve **38%** (manual 102→96 หลังเพิ่ม unwrapper + near-miss wording)
- **⛔ generic หมดทางแล้ว**: ตัวเลือกที่ยัง resolve ไม่ได้ 108 อัน มี **107 แบบที่ไม่ซ้ำกัน** (แบบที่ซ้ำสุดโผล่ 2 ครั้ง) → ต่อจากนี้เป็นงาน script รายใบ ไม่ใช่ pattern อีกแล้ว ให้ไปรวมกับคิว residual
- **หลักการ**: ถามก่อนเสมอ แม้ resolve อัตโนมัติไม่ได้ — ผู้เล่นต้องได้ตัดสินใจ และเห็นว่าการ์ดเขียนว่าอะไร

### Raid bug — ชื่อไม่ตรงกันระหว่างข้อความ [Raid] กับชื่อการ์ดจริง (2026-07-26)
ผู้ใช้แจ้งว่า Sukuna raid ทับ Itadori (Energy 2) ไม่ได้ — เจอบั๊ก **3 ตัวใน `raidTargetsFor`/`parseKeywords`**:
1. **ชื่อสะกด/เรียงต่างกัน** — `[Raid] <Itadori Yuji>` แต่การ์ดชื่อ `"Itadori Yuuji"` (uu) หรือ `"Yuji Itadori"` (สลับลำดับ) → `includes()` fail ทั้งคู่ → **Sukuna raid ทับอะไรไม่ได้เลย** กระทบ **27 ใบทั่วฐานข้อมูล** (BLC Abarai Renji, KMY Zenitsu/Inosuke, MHA Shoto/Ochaco, CGH Kouzuki Kallen ฯลฯ) แก้ด้วย `nameMatches()`: เทียบเป็น **token set แบบไม่สนลำดับ + ยุบสระยาว** (uu→u, oo→o, ou→o) **โดยยัง OR กับ `includes()` เดิม** เพื่อให้เพิ่มการแมตช์เท่านั้น ไม่ตัดของเดิม
2. **`<Trait: X>` ถูกอ่านเป็นชื่อการ์ด** — `[Raid] <Trait: Four Holy Swords>` อยู่ในวงเล็บแหลมเลยถูกจัดเป็น `kind:'name'` → ไม่เคยเจอ target (11 ใบ)
3. **จับแค่ตัวเลือกแรก** — `[Raid] <A> or <B>` เก็บแค่ A ทำให้ B ใช้ raid ไม่ได้ (9 ใบ)
- ทดสอบยืนยัน: Sukuna ทับ "Itadori Yuuji" ได้ ✓ · ทับ "Yuji Itadori" ได้ ✓ · ทับ character ที่ไม่เกี่ยวข้อง **ไม่ได้** ✓ (กัน over-match)

### UI: เลือกการ์ดจากรูป ไม่ใช่จากชื่อ (2026-07-26)
`modalChoice` รับ `card` ต่อปุ่มได้แล้ว → เรนเดอร์เป็น **tile รูปการ์ด** (`.choice-cards`/`.choice-card`) ส่วนปุ่มที่ไม่ใช่การ์ด (ข้าม/ไม่บล็อก/เมนู effect) ยังเป็นข้อความ
- แปลงครบ 10 จุด: chooseBlocker · chooseOwnCharacter · chooseEnemyCharacter · chooseCardFromHand · chooseCardsFromHand · chooseCardFromRemoval · chooseCardFromSideline · chooseDiscard · chooseRaidFromTrigger · chooseRevealPick
- `chooseRevealPick` ตัด preview ซ้ำออกเมื่อทุกใบที่เปิดเลือกได้หมด (ไม่งั้นการ์ดโผล่ 2 ที่)
- ทดสอบผ่าน browser จริง: 4 tile + ปุ่ม "ข้าม" เป็นข้อความ, รูปโหลด HTTP 200 (webp 240px) — `complete:false` ตอนแรกเป็นเพราะ `loading="lazy"` ไม่ยิงใน pane ที่ไม่ composite ไม่ใช่ 404

## ⚠️ กับดักสำคัญ (เคยพลาดมาแล้ว)

- **"Outside Area" ในข้อความการ์ด = โซน Sideline (`p.sideline`) ไม่ใช่ Removal Area!**
  `p.removal` ใช้เฉพาะ: ทิ้งมือเกิน 8 ใบ, line เต็ม 4 ต้องเอาออก, และข้อความที่เขียนว่า "Remove Area" ตรงๆ (หายาก)
- **ห้ามใช้ regex แบบ anchor ตายตัว** กับข้อความการ์ด — ข้อความเดียวกันสะกด/เรียงคำต่างกันนับสิบแบบ (place/put/discard, it/that card, top of/at the top, มี typo ในข้อมูลต้นทางด้วย) ใช้ keyword-based matcher ดู `findClause`/`findMatch` ใน common.js
- **clause คั่นด้วย `@`** และบางใบมี passive clause อยู่ก่อน `[On Play]` — ต้องหาทุก segment ไม่ใช่แค่ตัวแรก
- เด็ค: 50 ใบ series เดียว **สีเดียว** (กติกาที่ผู้ใช้กำหนดเพิ่มจาก manual), max 4/หมายเลข, trigger Special/Color/Final อย่างละ ≤4

## โครงสร้าง

| ไฟล์ | หน้าที่ |
|---|---|
| `js/game/engine.js` | กติกา + turn loop + `Effects` hook system (ท้ายไฟล์มี doc ของทุก hook) |
| `js/effects/common.js` | generic pattern ทุก series + `window.UAEffectHelpers` (helper ที่ script รายใบเรียกใช้) |
| `js/effects/mcr.js`, `eva.js`, `htr.js`, `ark.js`, `cgh.js`, `and.js`, `slg.js`, `jjk.js`, `blc.js`, `tsk.js`, `kmy.js`, `kgr.js`, `smd.js`, `gmr.js`, `blk.js`, `kmr.js`, `opm.js`, `kin.js`, `yyh.js`, `hiq.js`, `tlr.js`, `gim.js`, `snf.js`, `syn.js`, `ngr.js`, `trk.js`, `nik.js`, `bcv.js`, `rnk.js`, `toa.js`, `kjn.js`, `gnt.js`, `mst.js`, `shy.js`, `csm.js`, `aot.js`, `rly.js`, `kgd.js`, `rez.js`, `btr.js`, `mha.js`, `wbk.js`, `ims.js`, `kj8.js`, `mgs.js`, `mss.js`, `tkg.js`, `fma.js`, `sao.js`, `tkn.js`, `iys.js`, `bac.js`, `cgd.js`, `dst.js`, `mmm.js` | script รายใบ (registry per cardNo — override generic เสมอ) |
| `js/game/bot.js` | bot AI + `buildBotDeck(series)` |
| `js/game/ui.js` | กระดาน 3D + human controller (modal prompts) |
| `js/deckbuilder.js`, `js/app.js` | deck builder + เมนู |
| `data/cards.js` | การ์ดทั้งหมด 10,473 ใบ (generated — อย่าแก้มือ) |
| `docs/rules-summary.md` | สรุปกติกาจาก Official Rule Manual v1.1 |

Effect hooks ที่มีให้ใช้ (ลง `Effects.registry[cardNo]`): `onPlay onAttack onBlock(atkUnit) onBeingBlocked(atkUnit,blockerUnit) onBeforeLeaveField(leavingUnit,ctx,selfUnit) onEvent onMain onLeaveField onSideline(reason) onTurnStart onAttackPhaseStart onAttackPhaseEnd onRaided onColorTrigger onWinBattle onDefenderWinBattle(atkP,atkUnit) onAnyWinBattle onAnyLoseBattle onAnyAttack(atkUnit,selfUnit) onAnyPlay(playedUnit,selfUnit) onAnyUnblockedAttack(atkUnit,selfUnit) onAnyLeaveField(leftUnit,selfUnit) onAnyMove(movedUnit,selfUnit) onBeforeLeaveCounters(reason)` (async) + `bpBonus impactBonus genMod costMod frontGenBonus auraBp(owner,srcUnit,tgtUnit) canAttack(p,unit) canPlayFromHand(p,card)` (sync, คำนวณสด — `auraBp` ห้ามเรียก `Engine.bp()` ซ้ำ จะ recursion; `canAttack`/`canPlayFromHand` ต้องเช็คคู่กับ bot.js's candidate filter เสมอ)
Unit state: `bpMod` (หมดเทิร์น) `bpPersist` (ถึงต้นเทิร์นหน้า) `tempImpact tempDmg tempGen genPersist tempFrontGen frontGenPersist counters under retireAtEndOfMain retireAtEndOfTurn noBlock skipNextStand noRetire tempSnipe tempUnblockableBP tempUnblockableBPMin tempUnblockableNeedMin tempCanAttackFromEnergy tempUntargetable effectsNullified enteredTurn _grantedOnWinDraw _grantedOnWinActive _grantedAttackDraw _grantedUnblockedDraw _playedByEffect _watchers[] _movedThisTurn _movedByEffectThisTurn tempRaidable tempCannotMove tempCannotAttack tempCannotBlock tempDoubleAttack tempMustBeBlocked tempMustBlock _playedFromDeck _playedFromSideline _playedFromRemoval`
Player state: `p._playedTraitsThisTurn` (Set ของ trait ตัวพิมพ์เล็กที่เล่นจากมือเทิร์นนี้), `p._drewThisTurn` `p._getPlayedThisTurn` `p.extraDrawUsed` `p._blockEnergyToFrontNextMove` `p._grantedRaidDraw` `p._cannotUseEventsThisTurn` `p._cannotPlayFromHandThisTurn` `p._eventNeedsPlayedThisTurn` `p._returnedToHandThisTurn` `p._playedFromDeckThisTurn` `p._playedFromSidelineThisTurn` `p._cardsPlayedFromHandThisTurn`
Engine helpers: `sidelineUnit returnUnitToHand moveUnitFree playCardFromZone checkBpZero draw log payAP hasTextCostDiscount opponentOf bp parseKeywords activeAP hasEnergyFor scheduleDelayedAction payApForEffect energyGen dealDamage resolveTrigger`
Keyword flags (จาก `parseKeywords`, ถาวรไม่รีเซ็ตรายเทิร์น): `step snipe doubleAttack doubleBlock nullifyImpact impact dmg raidTargets entersActive entersActiveIf unblockableBP unblockableBPMin alsoTreatedAs frontGen untargetable cannotBlock cannotAttack unblockableByRaided cannotMove cannotEnterFront cannotEnterEnergy cannotMoveToFront raidableByAny retireToRemoval immuneBpReduction snipeMaxBP mustBeBlocked mustBlock`

## ทดสอบ

```bash
node tools/test-engine.mjs MCR EVA HTR      # bot vs bot 1 เกม ระบุ series
node tools/stress-test-all-series.mjs      # ทุก series — รันก่อน commit เสมอ
node tools/uncovered-in-series.mjs EVA     # การ์ดที่ยังไม่ครอบคลุมใน series
node tools/coverage-report-full.mjs        # ตารางทุก series + แยก "ยังไม่ครอบคลุมเลย" vs "covered จาก keyword แต่ยังมี bonus text ค้าง" (--json ได้)
python -m http.server 8777                 # dev server แล้วเปิด localhost:8777
```

## Data pipeline (ปกติไม่ต้องแตะ)

การ์ดจาก `https://auth.exburst.dev/rest/v1/ua_cards` (Supabase, anon key ใน tools/scrape-cards.mjs)
→ `tools/scrape-cards.mjs` → `tools/build-data-js.mjs` → `data/cards.js` · รูปใน `assets/cards/` (7,608 ไฟล์)

## งานอื่นที่ค้างในคิว (รองจาก effect)

- Co-Op เชิญเพื่อนเล่นผ่าน WebRTC (ยังไม่เริ่ม — ปุ่มในเมนูขึ้น "เร็วๆ นี้")
